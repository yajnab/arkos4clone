diff --git a/core/ui/gui.cpp b/core/ui/gui.cpp
index 739c956bf..e22f2e893 100644
--- a/core/ui/gui.cpp
+++ b/core/ui/gui.cpp
@@ -52,6 +52,7 @@
 #endif
 #include "vgamepad.h"
 #include "settings.h"
+#include "i18n.h"
 
 #ifdef _WIN32
 #include <windows.h>
@@ -324,7 +325,52 @@ void gui_initFonts()
         	io.Fonts->AddFontFromFileTTF("/system/fonts/NotoSansCJK-Regular.ttc", fontSize, &font_cfg, glyphRanges);
     }
 
-    // TODO Linux, iOS, ...
+#elif defined(__linux__)
+    {
+        setlocale(LC_ALL, "");
+        const char* locale = setlocale(LC_ALL, nullptr);
+        fprintf(stderr, "[FLYCAST] setlocale returned: %s\n", locale ? locale : "NULL");
+        if (locale == nullptr || strcmp(locale, "C") == 0 || strcmp(locale, "POSIX") == 0) {
+            locale = getenv("LANG");
+            fprintf(stderr, "[FLYCAST] getenv LANG: %s\n", locale ? locale : "NULL");
+        }
+        if (locale != nullptr) {
+            const ImWchar* glyphRanges = nullptr;
+            if (strstr(locale, "zh_CN") != nullptr || strstr(locale, "zh_cn") != nullptr) {
+                glyphRanges = GetGlyphRangesChineseSimplifiedOfficial();
+                fprintf(stderr, "[FLYCAST] Detected Simplified Chinese, loading font...\n");
+            }
+            else if (strstr(locale, "zh_TW") != nullptr || strstr(locale, "zh_HK") != nullptr ||
+                     strstr(locale, "zh_tw") != nullptr || strstr(locale, "zh_hk") != nullptr) {
+                glyphRanges = GetGlyphRangesChineseTraditionalOfficial();
+                fprintf(stderr, "[FLYCAST] Detected Traditional Chinese, loading font...\n");
+            }
+            else if (strstr(locale, "ja") != nullptr || strstr(locale, "JA") != nullptr)
+                glyphRanges = io.Fonts->GetGlyphRangesJapanese();
+            else if (strstr(locale, "ko") != nullptr || strstr(locale, "KO") != nullptr)
+                glyphRanges = io.Fonts->GetGlyphRangesKorean();
+            
+            if (glyphRanges != nullptr) {
+                // Use slightly larger font size for CJK characters for better readability
+                const float cjkFontSize = fontSize * 1.25f;
+                ImFont* font = io.Fonts->AddFontFromFileTTF("/usr/share/fonts/opentype/noto/NotoSansCJK-Regular.ttc", 
+                    cjkFontSize, &font_cfg, glyphRanges);
+                if (font == nullptr) {
+                    fprintf(stderr, "[FLYCAST] Failed to load font from /usr/share/fonts/opentype/noto/NotoSansCJK-Regular.ttc\n");
+                    font = io.Fonts->AddFontFromFileTTF("/usr/share/fonts/truetype/noto/NotoSansCJK-Regular.ttc",
+                        fontSize, &font_cfg, glyphRanges);
+                    if (font == nullptr)
+                        fprintf(stderr, "[FLYCAST] Failed to load font from /usr/share/fonts/truetype/noto/NotoSansCJK-Regular.ttc\n");
+                    else
+                        fprintf(stderr, "[FLYCAST] Font loaded from /usr/share/fonts/truetype/noto/NotoSansCJK-Regular.ttc\n");
+                } else {
+                    fprintf(stderr, "[FLYCAST] Font loaded from /usr/share/fonts/opentype/noto/NotoSansCJK-Regular.ttc\n");
+                }
+            } else {
+                fprintf(stderr, "[FLYCAST] No CJK locale detected, locale=%s\n", locale);
+            }
+        }
+    }
 #endif
 	// Font Awesome symbols (added to default font)
 	data = resource::load("fonts/" FONT_ICON_FILE_NAME_FAS, dataSize);
@@ -676,7 +722,7 @@ static void gui_display_commands()
 		ImguiStyleVar _1{ImGuiStyleVar_FramePadding, ScaledVec2(12.f, 3.f)};
 
 		// Resume
-		if (ImGui::Button(ICON_FA_PLAY "  Resume", ScaledVec2(buttonWidth, 50)))
+		if (ImGui::Button((ICON_FA_PLAY "  " + std::string(tr("Resume"))).c_str(), ScaledVec2(buttonWidth, 50)))
 		{
 			GamepadDevice::load_system_mappings();
 			gui_setState(GuiState::Closed);
@@ -685,14 +731,14 @@ static void gui_display_commands()
 		{
 			DisabledScope _{settings.network.online || settings.raHardcoreMode};
 
-			if (ImGui::Button(ICON_FA_MASK "  Cheats", ScaledVec2(buttonWidth, 50)) && !settings.network.online)
+			if (ImGui::Button((ICON_FA_MASK "  " + std::string(tr("Cheats"))).c_str(), ScaledVec2(buttonWidth, 50)) && !settings.network.online)
 				gui_setState(GuiState::Cheats);
 		}
 		// Achievements
 		{
 			DisabledScope _{!achievements::isActive()};
 
-			if (ImGui::Button(ICON_FA_TROPHY "  Achievements", ScaledVec2(buttonWidth, 50)) && achievements::isActive())
+			if (ImGui::Button((ICON_FA_TROPHY "  " + std::string(tr("Achievements"))).c_str(), ScaledVec2(buttonWidth, 50)) && achievements::isActive())
 				gui_setState(GuiState::Achievements);
 		}
 		// Barcode
@@ -727,7 +773,7 @@ static void gui_display_commands()
 		{
 			if (hasDisconnectedDreamLink)
 			{
-				if (ImGui::Button("Connect DreamLink", ScaledVec2(buttonWidth, 50)))
+				if (ImGui::Button(tr("Connect DreamLink"), ScaledVec2(buttonWidth, 50)))
 				{
 					if (reconnectDreamLinks())
 						maple_ReconnectDevices();
@@ -735,7 +781,7 @@ static void gui_display_commands()
 			}
 			else
 			{
-				if (ImGui::Button("Disconnect\nDreamLink", ScaledVec2(buttonWidth, 50)))
+				if (ImGui::Button(tr("Disconnect\nDreamLink"), ScaledVec2(buttonWidth, 50)))
 				{
 					for (auto& dreamlink : DreamLink::activeDreamLinks)
 					{
@@ -750,7 +796,7 @@ static void gui_display_commands()
 				auto dreamlink = DreamLink::activeDreamLinks[i];
 				if (dreamlink)
 				{
-					ImGui::Text("Port %c: %s", 'A' + i, dreamlink->isConnected() ? "Connected" : "Disconnected");
+					ImGui::Text("Port %c: %s", 'A' + i, dreamlink->isConnected() ? tr("Connected") : tr("Disconnected"));
 				}
 			}
 		}
@@ -759,8 +805,8 @@ static void gui_display_commands()
 		ImGui::NextColumn();
 
 		// Insert/Eject Disk
-		const char *disk_label = gdr::isOpen() ? ICON_FA_COMPACT_DISC "  Insert Disk" : ICON_FA_COMPACT_DISC "  Eject Disk";
-		if (ImGui::Button(disk_label, ScaledVec2(buttonWidth, 50)))
+		std::string disk_label_str = gdr::isOpen() ? (ICON_FA_COMPACT_DISC "  " + std::string(tr("Insert Disk"))) : (ICON_FA_COMPACT_DISC "  " + std::string(tr("Eject Disk")));
+		if (ImGui::Button(disk_label_str.c_str(), ScaledVec2(buttonWidth, 50)))
 		{
 			if (gdr::isOpen()) {
 				gui_setState(GuiState::SelectDisk);
@@ -771,11 +817,11 @@ static void gui_display_commands()
 			}
 		}
 		// Settings
-		if (ImGui::Button(ICON_FA_GEAR "  Settings", ScaledVec2(buttonWidth, 50)))
+		if (ImGui::Button((ICON_FA_GEAR "  " + std::string(tr("Settings"))).c_str(), ScaledVec2(buttonWidth, 50)))
 			gui_setState(GuiState::Settings);
 
 		// Exit
-		if (ImGui::Button(commandLineStart ? ICON_FA_POWER_OFF "  Exit" : ICON_FA_POWER_OFF "  Close Game", ScaledVec2(buttonWidth, 50)))
+		if (ImGui::Button(commandLineStart ? (ICON_FA_POWER_OFF "  " + std::string(tr("Exit"))).c_str() : (ICON_FA_POWER_OFF "  " + std::string(tr("Close Game"))).c_str(), ScaledVec2(buttonWidth, 50)))
 			gui_stop_game();
 
 		ImGui::NextColumn();
@@ -787,7 +833,7 @@ static void gui_display_commands()
 			// Load State
 			{
 				DisabledScope _{settings.raHardcoreMode || savestateDate == 0};
-				if (ImGui::Button(ICON_FA_CLOCK_ROTATE_LEFT "  Load State", ScaledVec2(buttonWidth, 50)) && savestateAllowed())
+				if (ImGui::Button((ICON_FA_CLOCK_ROTATE_LEFT "  " + std::string(tr("Load State"))).c_str(), ScaledVec2(buttonWidth, 50)) && savestateAllowed())
 				{
 					gui_setState(GuiState::Closed);
 					dc_loadstate(config::SavestateSlot);
@@ -795,7 +841,7 @@ static void gui_display_commands()
 			}
 
 			// Save State
-			if (ImGui::Button(ICON_FA_DOWNLOAD "  Save State", ScaledVec2(buttonWidth, 50)) && savestateAllowed())
+			if (ImGui::Button((ICON_FA_DOWNLOAD "  " + std::string(tr("Save State"))).c_str(), ScaledVec2(buttonWidth, 50)) && savestateAllowed())
 			{
 				gui_setState(GuiState::Closed);
 				savestate();
@@ -883,7 +929,7 @@ static void contentpath_warning_popup()
 				ImguiStyleVar _(ImGuiStyleVar_FramePadding, ScaledVec2(16, 3));
 				float currentwidth = ImGui::GetContentRegionAvail().x;
 				ImGui::SetCursorPosX((currentwidth - uiScaled(100.f)) / 2.f + ImGui::GetStyle().WindowPadding.x - uiScaled(55.f));
-				if (ImGui::Button("Reselect", ScaledVec2(100.f, 0)))
+				if (ImGui::Button(tr("Reselect"), ScaledVec2(100.f, 0)))
 				{
 					scanner.content_path_looks_incorrect = false;
 					ImGui::CloseCurrentPopup();
@@ -892,7 +938,7 @@ static void contentpath_warning_popup()
 
 				ImGui::SameLine();
 				ImGui::SetCursorPosX((currentwidth - uiScaled(100.f)) / 2.f + ImGui::GetStyle().WindowPadding.x + uiScaled(55.f));
-				if (ImGui::Button("Cancel", ScaledVec2(100.f, 0)))
+				if (ImGui::Button(tr("Cancel"), ScaledVec2(100.f, 0)))
 				{
 					scanner.content_path_looks_incorrect = false;
 					ImGui::CloseCurrentPopup();
@@ -1015,11 +1061,11 @@ static void gui_display_content()
     ImGui::PushStyleVar(ImGuiStyleVar_FramePadding, ScaledVec2(20, 8));
     ImGui::AlignTextToFramePadding();
     ImGui::Indent(uiScaled(10));
-    ImGui::Text("GAMES");
+    ImGui::Text(tr("GAMES"));
     ImGui::Unindent(uiScaled(10));
 
     static ImGuiTextFilter filter;
-    const float settingsBtnW = iconButtonWidth(ICON_FA_GEAR, "Settings");
+    const float settingsBtnW = iconButtonWidth(ICON_FA_GEAR, tr("Settings"));
 #if !defined(__ANDROID__) && !defined(TARGET_IPHONE) && !defined(TARGET_UWP) && !defined(__SWITCH__)
 	ImGui::SameLine(0, uiScaled(32));
 	filter.Draw("Filter", ImGui::GetContentRegionAvail().x - ImGui::GetStyle().ItemSpacing.x - uiScaled(32)
@@ -1035,21 +1081,21 @@ static void gui_display_content()
 		ImGui::SameLine();
 #elif defined(__SWITCH__)
 		ImGui::SameLine(ImGui::GetContentRegionMax().x - settingsBtnW
-				- ImGui::GetStyle().ItemSpacing.x - iconButtonWidth(ICON_FA_POWER_OFF, "Exit"));
-		if (iconButton(ICON_FA_POWER_OFF, "Exit"))
+				- ImGui::GetStyle().ItemSpacing.x - iconButtonWidth(ICON_FA_POWER_OFF, tr("Exit")));
+		if (iconButton(ICON_FA_POWER_OFF, tr("Exit")))
 			dc_exit();
 		ImGui::SameLine();
 #else
 		ImGui::SameLine(ImGui::GetContentRegionMax().x - settingsBtnW);
 #endif
-		if (iconButton(ICON_FA_GEAR, "Settings"))
+		if (iconButton(ICON_FA_GEAR, tr("Settings")))
 			gui_setState(GuiState::Settings);
     }
     else
     {
 		ImGui::SameLine(ImGui::GetContentRegionMax().x
 				- ImGui::GetStyle().FramePadding.x * 2.0f - ImGui::CalcTextSize("Cancel").x);
-		if (ImGui::Button("Cancel"))
+		if (ImGui::Button(tr("Cancel")))
 			gui_setState(GuiState::Commands);
     }
     ImGui::PopStyleVar();
@@ -1283,7 +1329,7 @@ static void gui_network_start()
 
 		float currentwidth = ImGui::GetContentRegionAvail().x;
 		ImGui::SetCursorPosX((currentwidth - uiScaled(100.f)) / 2.f + ImGui::GetStyle().WindowPadding.x);
-		if (ImGui::Button("Cancel", ScaledVec2(100.f, 0)) && NetworkHandshake::instance != nullptr)
+		if (ImGui::Button(tr("Cancel"), ScaledVec2(100.f, 0)) && NetworkHandshake::instance != nullptr)
 		{
 			NetworkHandshake::instance->stop();
 			try {
@@ -1426,7 +1472,7 @@ static void gui_display_loadscreen()
 
 				float currentwidth = ImGui::GetContentRegionAvail().x;
 				ImGui::SetCursorPosX((currentwidth - uiScaled(100.f)) / 2.f + ImGui::GetStyle().WindowPadding.x);
-				if (ImGui::Button("Cancel", ScaledVec2(100.f, 0)))
+				if (ImGui::Button(tr("Cancel"), ScaledVec2(100.f, 0)))
 					gameLoader.cancel();
 			}
 		} catch (const FlycastException& ex) {
diff --git a/core/ui/settings.cpp b/core/ui/settings.cpp
index 4b6a3a7c6..d6f8c7216 100644
--- a/core/ui/settings.cpp
+++ b/core/ui/settings.cpp
@@ -23,6 +23,7 @@
 #include "log/LogManager.h"
 #include "hw/maple/maple_if.h"
 #include "imgui_stdlib.h"
+#include "i18n.h"
 
 #ifdef GDB_SERVER
 #include "hw/mem/addrspace.h"
@@ -35,23 +36,23 @@
 static void gui_settings_advanced()
 {
 #if FEAT_SHREC != DYNAREC_NONE
-    header("CPU Mode");
+    header(tr("CPU Mode"));
     {
 		ImGui::Columns(2, "cpu_modes", false);
-		OptionRadioButton("Dynarec", config::DynarecEnabled, true,
-			"Use the dynamic recompiler. Recommended in most cases");
+		OptionRadioButton(tr("Dynarec"), config::DynarecEnabled, true,
+			tr("Use the dynamic recompiler. Recommended in most cases"));
 		ImGui::NextColumn();
-		OptionRadioButton("Interpreter", config::DynarecEnabled, false,
-			"Use the interpreter. Very slow but may help in case of a dynarec problem");
+		OptionRadioButton(tr("Interpreter"), config::DynarecEnabled, false,
+			tr("Use the interpreter. Very slow but may help in case of a dynarec problem"));
 		ImGui::Columns(1, NULL, false);
 
-		OptionSlider("SH4 Clock", config::Sh4Clock, 100, 300,
+		OptionSlider(tr("SH4 Clock"), config::Sh4Clock, 100, 300,
 				"Over/Underclock the main SH4 CPU. Default is 200 MHz. Other values may crash, freeze or trigger unexpected nuclear reactions.",
 				"%d MHz");
     }
 #ifdef GDB_SERVER
 	ImGui::Spacing();
-	header("Virtual memory addresses");
+	header(tr("Virtual memory addresses"));
 	{
 		void *ram_base, *ram, *vram, *aram;
 		addrspace::getAddress(&ram_base, &ram, &vram, &aram);
@@ -73,13 +74,13 @@ static void gui_settings_advanced()
 
 	}
 	ImGui::Spacing();
-	header("Debugging");
+	header(tr("Debugging"));
 	{
-		OptionCheckbox("Enable GDB", config::GDB, "GDB debugging support, disables Dynarec and dramatically reduces performance when a debugger is connected.");
-		OptionCheckbox("Wait for connection", config::GDBWaitForConnection, "Start emulation once the debugger is connected.");
+		OptionCheckbox(tr("Enable GDB"), config::GDB, "GDB debugging support, disables Dynarec and dramatically reduces performance when a debugger is connected.");
+		OptionCheckbox(tr("Wait for connection"), config::GDBWaitForConnection, "Start emulation once the debugger is connected.");
 #ifndef __ANDROID
-		OptionCheckbox("Serial Console", config::SerialConsole, "Dump the Dreamcast serial console to stdout");
-		OptionCheckbox("Serial PTY", config::SerialPTY, "Requires the option \"Serial Console\" to work");
+		OptionCheckbox(tr("Serial Console"), config::SerialConsole, "Dump the Dreamcast serial console to stdout");
+		OptionCheckbox(tr("Serial PTY"), config::SerialPTY, "Requires the option \"Serial Console\" to work");
 #endif
 
 		static int gdbport = config::GDBPort;
@@ -94,26 +95,26 @@ static void gui_settings_advanced()
 #endif
 	ImGui::Spacing();
 #endif
-    header("Other");
+    header(tr("Other"));
     {
-    	OptionCheckbox("HLE BIOS", config::UseReios, "Force high-level BIOS emulation");
-        OptionCheckbox("Multi-threaded emulation", config::ThreadedRendering,
+    	OptionCheckbox(tr("HLE BIOS"), config::UseReios, "Force high-level BIOS emulation");
+        OptionCheckbox(tr("Multi-threaded emulation"), config::ThreadedRendering,
         		"Run the emulated CPU and GPU on different threads");
 #if !defined(__ANDROID) && !defined(GDB_SERVER)
-        OptionCheckbox("Serial Console", config::SerialConsole,
+        OptionCheckbox(tr("Serial Console"), config::SerialConsole,
         		"Dump the Dreamcast serial console to stdout");
 #endif
 		{
 			DisabledScope scope(game_started);
-			OptionCheckbox("Dreamcast 32MB RAM Mod", config::RamMod32MB,
+			OptionCheckbox(tr("Dreamcast 32MB RAM Mod"), config::RamMod32MB,
 				"Enables 32MB RAM Mod for Dreamcast. May affect compatibility");
 		}
-        OptionCheckbox("Dump Textures", config::DumpTextures,
+        OptionCheckbox(tr("Dump Textures"), config::DumpTextures,
         		"Dump all textures into data/texdump/<game id>");
 		ImGui::Indent();
 		{
 			DisabledScope scope(!config::DumpTextures.get());
-			OptionCheckbox("Dump Replaced Textures", config::DumpReplacedTextures,
+			OptionCheckbox(tr("Dump Replaced Textures"), config::DumpReplacedTextures,
 					"Always dump textures that are already replaced by custom textures");
 		}
 		ImGui::Unindent();
@@ -123,13 +124,13 @@ static void gui_settings_advanced()
         ImGui::SameLine();
         ShowHelpMarker("Log debug information to flycast.log");
 #ifdef SENTRY_UPLOAD
-        OptionCheckbox("Automatically Report Crashes", config::UploadCrashLogs,
+        OptionCheckbox(tr("Automatically Report Crashes"), config::UploadCrashLogs,
         		"Automatically upload crash reports to sentry.io to help in troubleshooting. No personal information is included.");
 #endif
     }
 
 #ifdef USE_LUA
-	header("Lua Scripting");
+	header(tr("Lua Scripting"));
 	{
 		InputText("Lua Filename", &config::LuaFileName.get(), ImGuiInputTextFlags_CharsNoBlank);
 		ImGui::SameLine();
@@ -142,7 +143,7 @@ static void gui_settings_advanced()
 
 static void gui_debug_tab()
 {
-	header("Logging");
+	header(tr("Logging"));
 	{
 		LogManager *logManager = LogManager::GetInstance();
 		for (LogTypes::LOG_TYPE type = LogTypes::AICA; type < LogTypes::NUMBER_OF_LOGS; type = (LogTypes::LOG_TYPE)(type + 1))
@@ -177,17 +178,17 @@ static void gui_debug_tab()
 	}
 #if FC_PROFILER
 	ImGui::Spacing();
-	header("Profiling");
+	header(tr("Profiling"));
 	{
 
-		OptionCheckbox("Enable", config::ProfilerEnabled, "Enable the profiler.");
+		OptionCheckbox(tr("Enable"), config::ProfilerEnabled, "Enable the profiler.");
 		if (!config::ProfilerEnabled)
 		{
 			ImGui::PushItemFlag(ImGuiItemFlags_Disabled, true);
 			ImGui::PushStyleVar(ImGuiStyleVar_Alpha, ImGui::GetStyle().Alpha * 0.5f);
 		}
-		OptionCheckbox("Display", config::ProfilerDrawToGUI, "Draw the profiler output in an overlay.");
-		OptionCheckbox("Output to terminal", config::ProfilerOutputTTY, "Write the profiler output to the terminal");
+		OptionCheckbox(tr("Display"), config::ProfilerDrawToGUI, "Draw the profiler output in an overlay.");
+		OptionCheckbox(tr("Output to terminal"), config::ProfilerOutputTTY, "Write the profiler output to the terminal");
 		// TODO frame warning time
 		if (!config::ProfilerEnabled)
 		{
@@ -210,7 +211,7 @@ void gui_display_settings()
     		| ImGuiWindowFlags_NoMove | ImGuiWindowFlags_NoCollapse);
 	ImVec2 normal_padding = ImGui::GetStyle().FramePadding;
 
-    if (ImGui::Button("Done", ScaledVec2(100, 30)))
+    if (ImGui::Button(tr("Done"), ScaledVec2(100, 30)))
     {
     	if (uiUserScaleUpdated)
     	{
@@ -241,7 +242,7 @@ void gui_display_settings()
 		ImguiStyleVar _(ImGuiStyleVar_FramePadding, ImVec2(uiScaled(16), normal_padding.y));
 		if (config::Settings::instance().hasPerGameConfig())
 		{
-			if (ImGui::Button("Delete Game Config", ScaledVec2(0, 30)))
+			if (ImGui::Button(tr("Delete Game Config"), ScaledVec2(0, 30)))
 			{
 				config::Settings::instance().setPerGameConfig(false);
 				config::Settings::instance().load(false);
@@ -250,7 +251,7 @@ void gui_display_settings()
 		}
 		else
 		{
-			if (ImGui::Button("Make Game Config", ScaledVec2(0, 30)))
+			if (ImGui::Button(tr("Make Game Config"), ScaledVec2(0, 30)))
 				config::Settings::instance().setPerGameConfig(true);
 		}
 	}
@@ -263,37 +264,37 @@ void gui_display_settings()
 
     if (ImGui::BeginTabBar("settings", ImGuiTabBarFlags_NoTooltip))
     {
-		if (ImGui::BeginTabItem(ICON_FA_TOOLBOX " General"))
+		if (ImGui::BeginTabItem((ICON_FA_TOOLBOX " " + std::string(tr("General"))).c_str()))
 		{
 			ImguiStyleVar _(ImGuiStyleVar_FramePadding, normal_padding);
 			gui_settings_general();
 			ImGui::EndTabItem();
 		}
-		if (ImGui::BeginTabItem(ICON_FA_GAMEPAD " Controls"))
+		if (ImGui::BeginTabItem((ICON_FA_GAMEPAD " " + std::string(tr("Controls"))).c_str()))
 		{
 			ImguiStyleVar _(ImGuiStyleVar_FramePadding, normal_padding);
 			gui_settings_controls(maple_devices_changed);
 			ImGui::EndTabItem();
 		}
-		if (ImGui::BeginTabItem(ICON_FA_DISPLAY " Video"))
+		if (ImGui::BeginTabItem((ICON_FA_DISPLAY " " + std::string(tr("Video"))).c_str()))
 		{
 			ImguiStyleVar _(ImGuiStyleVar_FramePadding, normal_padding);
 			gui_settings_video();
 			ImGui::EndTabItem();
 		}
-		if (ImGui::BeginTabItem(ICON_FA_MUSIC " Audio"))
+		if (ImGui::BeginTabItem((ICON_FA_MUSIC " " + std::string(tr("Audio"))).c_str()))
 		{
 			ImguiStyleVar _(ImGuiStyleVar_FramePadding, normal_padding);
 			gui_settings_audio();
 			ImGui::EndTabItem();
 		}
-		if (ImGui::BeginTabItem(ICON_FA_WIFI " Network"))
+		if (ImGui::BeginTabItem((ICON_FA_WIFI " " + std::string(tr("Network"))).c_str()))
 		{
 			ImguiStyleVar _(ImGuiStyleVar_FramePadding, normal_padding);
 			gui_settings_network();
 			ImGui::EndTabItem();
 		}
-		if (ImGui::BeginTabItem(ICON_FA_MICROCHIP " Advanced"))
+		if (ImGui::BeginTabItem((ICON_FA_MICROCHIP " " + std::string(tr("Advanced"))).c_str()))
 		{
 			ImguiStyleVar _(ImGuiStyleVar_FramePadding, normal_padding);
 			gui_settings_advanced();
diff --git a/core/ui/settings_about.cpp b/core/ui/settings_about.cpp
index e686e8bc4..e88bc0661 100644
--- a/core/ui/settings_about.cpp
+++ b/core/ui/settings_about.cpp
@@ -17,6 +17,7 @@
     along with Flycast.  If not, see <https://www.gnu.org/licenses/>.
  */
 #include "settings.h"
+#include "i18n.h"
 #include "gui.h"
 #include "version.h"
 #include "wsi/context.h"
@@ -50,14 +51,14 @@ void gui_settings_about()
 {
     header("Flycast");
     {
-		ImGui::Text("Version: %s", GIT_VERSION);
-		ImGui::Text("Git Hash: %s", GIT_HASH);
-		ImGui::Text("Build Date: %s", BUILD_DATE);
+		ImGui::Text(tr("Version: %s"), GIT_VERSION);
+		ImGui::Text(tr("Git Hash: %s"), GIT_HASH);
+		ImGui::Text(tr("Build Date: %s"), BUILD_DATE);
     }
 	ImGui::Spacing();
-    header("Platform");
+    header(tr("Platform"));
     {
-    	ImGui::Text("CPU: %s",
+    	ImGui::Text(tr("CPU: %s"),
 #if HOST_CPU == CPU_X86
 			"x86"
 #elif HOST_CPU == CPU_ARM
@@ -70,7 +71,7 @@ void gui_settings_about()
 			"Unknown"
 #endif
 				);
-    	ImGui::Text("Operating System: %s",
+    	ImGui::Text(tr("Operating System: %s"),
 #ifdef __ANDROID__
 			"Android"
 #elif defined(__unix__)
@@ -93,18 +94,18 @@ void gui_settings_about()
 				);
 #ifdef TARGET_IPHONE
 		const char *getIosJitStatus();
-		ImGui::Text("JIT Status: %s", getIosJitStatus());
+		ImGui::Text(tr("JIT Status: %s"), getIosJitStatus());
 #endif
     }
 	ImGui::Spacing();
 	if (isOpenGL(config::RendererType))
-		header("OpenGL");
+		header(tr("OpenGL"));
 	else if (isVulkan(config::RendererType))
-		header("Vulkan");
+		header(tr("Vulkan"));
 	else if (isDirectX(config::RendererType))
-		header("DirectX");
-	ImGui::Text("Driver Name: %s", GraphicsContext::Instance()->getDriverName().c_str());
-	ImGui::Text("Version: %s", GraphicsContext::Instance()->getDriverVersion().c_str());
+		header(tr("DirectX"));
+	ImGui::Text(tr("Driver Name: %s"), GraphicsContext::Instance()->getDriverName().c_str());
+	ImGui::Text(tr("Version: %s"), GraphicsContext::Instance()->getDriverVersion().c_str());
 
 #if defined(__ANDROID__) && HOST_CPU == CPU_ARM64 && USE_VULKAN
 	if (isVulkan(config::RendererType))
@@ -117,7 +118,7 @@ void gui_settings_about()
 				std::string name, description, vendor, version;
 				if (getCustomGpuDriverInfo(name, description, vendor, version))
 				{
-					ImGui::Text("Custom Driver:");
+					ImGui::Text(tr("Custom Driver:"));
 					ImGui::Indent();
 					ImGui::Text("%s - %s", name.c_str(), description.c_str());
 					ImGui::Text("%s - %s", vendor.c_str(), version.c_str());
@@ -142,7 +143,7 @@ void gui_settings_about()
 			ImguiStyleVar _1(ImGuiStyleVar_WindowPadding, ScaledVec2(20, 20));
 			if (ImGui::BeginPopupModal("Reset Vulkan", NULL, ImGuiWindowFlags_AlwaysAutoResize | ImGuiWindowFlags_NoMove | ImGuiWindowFlags_NoScrollbar))
 			{
-				ImGui::Text("Do you want to reset Vulkan to use new driver?");
+				ImGui::Text(tr("Do you want to reset Vulkan to use new driver?"));
 				ImGui::NewLine();
 				ImguiStyleVar _(ImGuiStyleVar_ItemSpacing, ImVec2(uiScaled(20), ImGui::GetStyle().ItemSpacing.y));
 				ImguiStyleVar _1(ImGuiStyleVar_FramePadding, ScaledVec2(10, 10));
diff --git a/core/ui/settings_audio.cpp b/core/ui/settings_audio.cpp
index b1e815537..085dc0c3a 100644
--- a/core/ui/settings_audio.cpp
+++ b/core/ui/settings_audio.cpp
@@ -17,22 +17,23 @@
     along with Flycast.  If not, see <https://www.gnu.org/licenses/>.
  */
 #include "settings.h"
+#include "i18n.h"
 #include "gui.h"
 #include "audio/audiostream.h"
 
 void gui_settings_audio()
 {
-	OptionCheckbox("Enable DSP", config::DSPEnabled,
+	OptionCheckbox(tr("Enable DSP"), config::DSPEnabled,
 			"Enable the Dreamcast Digital Sound Processor. Only recommended on fast platforms");
-    OptionCheckbox("Enable VMU Sounds", config::VmuSound, "Play VMU beeps when enabled.");
+    OptionCheckbox(tr("Enable VMU Sounds"), config::VmuSound, "Play VMU beeps when enabled.");
 
-	if (OptionSlider("Volume Level", config::AudioVolume, 0, 100, "Adjust the emulator's audio level", "%d%%"))
+	if (OptionSlider(tr("Volume Level"), config::AudioVolume, 0, 100, tr("Adjust the emulator's audio level"), "%d%%"))
 	{
 		config::AudioVolume.calcDbPower();
 	};
 #ifdef __ANDROID__
 	if (config::AudioBackend.get() == "auto" || config::AudioBackend.get() == "android")
-		OptionCheckbox("Automatic Latency", config::AutoLatency,
+		OptionCheckbox(tr("Automatic Latency"), config::AutoLatency,
 				"Automatically set audio latency. Recommended");
 #endif
     if (!config::AutoLatency
diff --git a/core/ui/settings_controls.cpp b/core/ui/settings_controls.cpp
index 49a5835c5..401d6105d 100644
--- a/core/ui/settings_controls.cpp
+++ b/core/ui/settings_controls.cpp
@@ -17,6 +17,7 @@
     along with Flycast.  If not, see <https://www.gnu.org/licenses/>.
  */
 #include "settings.h"
+#include "i18n.h"
 #include "gui.h"
 #include "IconsFontAwesome6.h"
 #include "input/gamepad_device.h"
@@ -382,7 +383,7 @@ static void detect_input_popup(const Mapping *mapping)
 			// Display currently detected buttons during the countdown
 			if (!mapped_codes.empty())
 			{
-				ImGui::Text("Current inputs: ");
+				ImGui::Text(tr("Current inputs: "));
 				ImGui::SameLine();
 				bool first = true;
 				for (const InputMapping::InputDef& inputDef : mapped_codes)
@@ -567,7 +568,7 @@ static void controller_mapping_popup(const std::shared_ptr<GamepadDevice>& gamep
 		}
 
 		std::shared_ptr<InputMapping> input_mapping = gamepad->get_input_mapping();
-		if (input_mapping == NULL || ImGui::Button("Done", ScaledVec2(100, 30)))
+		if (input_mapping == NULL || ImGui::Button(tr("Done"), ScaledVec2(100, 30)))
 		{
 			ImGui::CloseCurrentPopup();
 			gamepad->save_mapping(map_system);
@@ -604,7 +605,7 @@ static void controller_mapping_popup(const std::shared_ptr<GamepadDevice>& gamep
 		float comboWidth = ImGui::CalcTextSize("Dreamcast Controls").x + ImGui::GetStyle().ItemSpacing.x + ImGui::GetFontSize() + ImGui::GetStyle().FramePadding.x * 4;
 		float gameConfigWidth = 0;
 		if (!settings.content.gameId.empty())
-			gameConfigWidth = ImGui::CalcTextSize(gamepad->isPerGameMapping() ? "Delete Game Config" : "Make Game Config").x + ImGui::GetStyle().ItemSpacing.x + ImGui::GetStyle().FramePadding.x * 2;
+			gameConfigWidth = ImGui::CalcTextSize(gamepad->isPerGameMapping() ? tr("Delete Game Config") : tr("Make Game Config")).x + ImGui::GetStyle().ItemSpacing.x + ImGui::GetStyle().FramePadding.x * 2;
 		ImGui::SameLine(0, ImGui::GetContentRegionAvail().x - comboWidth - gameConfigWidth - ImGui::GetStyle().ItemSpacing.x - uiScaled(100) * 2 - portWidth);
 
 		ImGui::AlignTextToFramePadding();
@@ -613,7 +614,7 @@ static void controller_mapping_popup(const std::shared_ptr<GamepadDevice>& gamep
 		{
 			if (gamepad->isPerGameMapping())
 			{
-				if (ImGui::Button("Delete Game Config", ScaledVec2(0, 30)))
+				if (ImGui::Button(tr("Delete Game Config"), ScaledVec2(0, 30)))
 				{
 					gamepad->setPerGameMapping(false);
 					if (!gamepad->find_mapping(map_system))
@@ -622,7 +623,7 @@ static void controller_mapping_popup(const std::shared_ptr<GamepadDevice>& gamep
 			}
 			else
 			{
-				if (ImGui::Button("Make Game Config", ScaledVec2(0, 30)))
+				if (ImGui::Button(tr("Make Game Config"), ScaledVec2(0, 30)))
 					gamepad->setPerGameMapping(true);
 			}
 			ImGui::SameLine();
@@ -634,11 +635,11 @@ static void controller_mapping_popup(const std::shared_ptr<GamepadDevice>& gamep
 			ImguiStyleVar _(ImGuiStyleVar_WindowPadding, ScaledVec2(20, 20));
 			if (ImGui::BeginPopupModal("Confirm Reset", NULL, ImGuiWindowFlags_AlwaysAutoResize | ImGuiWindowFlags_NoMove))
 			{
-				ImGui::Text("Are you sure you want to reset the mappings to default?");
+				ImGui::Text(tr("Are you sure you want to reset the mappings to default?"));
 				static bool hitbox;
 				if (arcade_button_mode)
 				{
-					ImGui::Text("Controller Type:");
+					ImGui::Text(tr("Controller Type:"));
 					if (ImGui::RadioButton("Gamepad", !hitbox))
 						hitbox = false;
 					ImGui::SameLine();
@@ -813,7 +814,7 @@ static void gamepadSettingsPopup(const std::shared_ptr<GamepadDevice>& gamepad)
 	ImguiStyleVar _(ImGuiStyleVar_WindowRounding, 0);
 	if (ImGui::BeginPopupModal("Gamepad Settings", NULL, ImGuiWindowFlags_NoResize | ImGuiWindowFlags_NoMove | ImGuiWindowFlags_DragScrolling))
 	{
-		if (ImGui::Button("Done", ScaledVec2(100, 30)))
+		if (ImGui::Button(tr("Done"), ScaledVec2(100, 30)))
 		{
 			gamepad->save_mapping();
 			// Update both console and arcade profile/mapping
@@ -855,11 +856,11 @@ static void gamepadSettingsPopup(const std::shared_ptr<GamepadDevice>& gamepad)
 		if (gamepad->is_virtual_gamepad())
 		{
 			if (gamepad->is_rumble_enabled()) {
-				header("Haptic");
-				OptionSlider("Power", config::VirtualGamepadVibration, 0, 100, "Haptic feedback power", "%d%%");
+				header(tr("Haptic"));
+				OptionSlider(tr("Power"), config::VirtualGamepadVibration, 0, 100, tr("Haptic feedback power"), "%d%%");
 			}
-			header("View");
-			OptionSlider("Transparency", config::VirtualGamepadTransparency, 0, 100, "Virtual gamepad buttons transparency", "%d%%");
+			header(tr("View"));
+			OptionSlider(tr("Transparency"), config::VirtualGamepadTransparency, 0, 100, tr("Virtual gamepad buttons transparency"), "%d%%");
 
 #if defined(__ANDROID__) || defined(TARGET_IPHONE)
 			vgamepad::ImguiVGamepadTexture tex;
@@ -889,7 +890,7 @@ static void gamepadSettingsPopup(const std::shared_ptr<GamepadDevice>& gamepad)
 		}
 		else if (gamepad->is_rumble_enabled())
 		{
-			header("Rumble");
+			header(tr("Rumble"));
 			int power = gamepad->get_rumble_power();
 			ImGui::SetNextItemWidth(uiScaled(300));
 			if (ImGui::SliderInt("Power", &power, 0, 100, "%d%%"))
@@ -899,7 +900,7 @@ static void gamepadSettingsPopup(const std::shared_ptr<GamepadDevice>& gamepad)
 		}
 		if (gamepad->has_analog_stick())
 		{
-			header("Thumbsticks");
+			header(tr("Thumbsticks"));
 			int deadzone = std::round(gamepad->get_dead_zone() * 100.f);
 			ImGui::SetNextItemWidth(uiScaled(300));
 			if (ImGui::SliderInt("Dead zone", &deadzone, 0, 100, "%d%%"))
@@ -922,7 +923,7 @@ static void gamepadSettingsPopup(const std::shared_ptr<GamepadDevice>& gamepad)
 
 void gui_settings_controls(bool& maple_devices_changed)
 {
-	header("Physical Devices");
+	header(tr("Physical Devices"));
     {
 		if (ImGui::BeginTable("physicalDevices", 5, ImGuiTableFlags_SizingFixedFit | ImGuiTableFlags_NoSavedSettings))
 		{
@@ -937,13 +938,13 @@ void gui_settings_controls(bool& maple_devices_changed)
 
 			ImGui::TableNextRow();
 			ImGui::TableSetColumnIndex(0);
-			ImGui::TextColored(gray, "System");
+			ImGui::TextColored(gray, tr("System"));
 
 			ImGui::TableSetColumnIndex(1);
-			ImGui::TextColored(gray, "Name");
+			ImGui::TextColored(gray, tr("Name"));
 
 			ImGui::TableSetColumnIndex(3);
-			ImGui::TextColored(gray, "Port");
+			ImGui::TextColored(gray, tr("Port"));
 
 			for (int i = 0; i < GamepadDevice::GetGamepadCount(); i++)
 			{
@@ -961,7 +962,7 @@ void gui_settings_controls(bool& maple_devices_changed)
 				ImGui::TableSetColumnIndex(2);
 				DreamLinkGamepad* dreamLinkGamepad = dynamic_cast<DreamLinkGamepad*>(gamepad.get());
 				if (dreamLinkGamepad != nullptr) {
-					ImGui::Text("DreamLink status: %s", dreamLinkGamepad->dreamLinkStatus());
+					ImGui::Text(tr("DreamLink status: %s"), dreamLinkGamepad->dreamLinkStatus());
 				}
 #endif
 
@@ -1024,21 +1025,21 @@ void gui_settings_controls(bool& maple_devices_changed)
     }
 
 	ImGui::Spacing();
-	OptionSlider("Mouse sensitivity", config::MouseSensitivity, 1, 500);
+	OptionSlider(tr("Mouse sensitivity"), config::MouseSensitivity, 1, 500);
 #if defined(_WIN32) && !defined(TARGET_UWP)
-	OptionCheckbox("Use Raw Input", config::UseRawInput, "Supports multiple pointing devices (mice, light guns) and keyboards");
+	OptionCheckbox(tr("Use Raw Input"), config::UseRawInput, "Supports multiple pointing devices (mice, light guns) and keyboards");
 #endif
 #ifdef USE_DREAMLINK_DEVICES
 	{
 		DisabledScope scope(game_started);
-		OptionCheckbox("Use Physical VMU Memory", config::UsePhysicalVmuMemory,
+		OptionCheckbox(tr("Use Physical VMU Memory"), config::UsePhysicalVmuMemory,
 			"Enables direct read/write access to physical VMU memory via DreamPicoPort/DreamConn. "
 			"This is not compatible with load state events.");
 	}
 #endif
 
 	ImGui::Spacing();
-	header("Dreamcast Devices");
+	header(tr("Dreamcast Devices"));
     {
 		bool is_there_any_xhair = false;
 		if (ImGui::BeginTable("dreamcastDevices", 4, ImGuiTableFlags_SizingFixedFit | ImGuiTableFlags_NoSavedSettings,
@@ -1051,7 +1052,7 @@ void gui_settings_controls(bool& maple_devices_changed)
 			{
 				ImGui::TableNextRow();
 				ImGui::TableSetColumnIndex(0);
-				ImGui::Text("Port %c", bus + 'A');
+				ImGui::Text(tr("Port %c"), bus + 'A');
 
 				ImGui::TableSetColumnIndex(1);
 				char device_name[32];
@@ -1150,7 +1151,7 @@ void gui_settings_controls(bool& maple_devices_changed)
 				{
 					ImGui::SameLine();
 					ImGui::PushID(bus);
-					bool pressed = OptionCheckbox("Use Network Expansion Devices", config::UseNetworkExpansionDevices[bus],
+					bool pressed = OptionCheckbox(tr("Use Network Expansion Devices"), config::UseNetworkExpansionDevices[bus],
 						"Connect to expansion devices such as VMUs over local TCP.");
 					ImGui::PopID();
 
@@ -1165,8 +1166,8 @@ void gui_settings_controls(bool& maple_devices_changed)
 		}
 		{
 			DisabledScope scope(!is_there_any_xhair);
-			OptionSlider("Crosshair Size", config::CrosshairSize, 10, 100);
+			OptionSlider(tr("Crosshair Size"), config::CrosshairSize, 10, 100);
 		}
-		OptionCheckbox("Per Game VMU A1", config::PerGameVmu, "When enabled, each game has its own VMU on port 1 of controller A.");
+		OptionCheckbox(tr("Per Game VMU A1"), config::PerGameVmu, "When enabled, each game has its own VMU on port 1 of controller A.");
     }
 }
diff --git a/core/ui/settings_general.cpp b/core/ui/settings_general.cpp
index c406be7ad..4e2bb603c 100644
--- a/core/ui/settings_general.cpp
+++ b/core/ui/settings_general.cpp
@@ -24,6 +24,7 @@
 #include "stdclass.h"
 #include "achievements/achievements.h"
 #include "imgui_stdlib.h"
+#include "i18n.h"
 
 static std::vector<std::string>* g_currentPathList = nullptr;
 static void managePathListCallback(std::string selection)
@@ -188,18 +189,18 @@ void gui_settings_general()
 		DisabledScope scope(settings.platform.isArcade());
 
 		const char *languages[] = { "Japanese", "English", "German", "French", "Spanish", "Italian", "Default" };
-		OptionComboBox("Language", config::Language, languages, std::size(languages),
+		OptionComboBox(tr("Language"), config::Language, languages, std::size(languages),
 			"The language as configured in the Dreamcast BIOS");
 
 		const char *broadcast[] = { "NTSC", "PAL", "PAL/M", "PAL/N", "Default" };
-		OptionComboBox("Broadcast", config::Broadcast, broadcast, std::size(broadcast),
+		OptionComboBox(tr("Broadcast"), config::Broadcast, broadcast, std::size(broadcast),
 				"TV broadcasting standard for non-VGA modes");
 	}
 
 	const char *consoleRegion[] = { "Japan", "USA", "Europe", "Default" };
 	const char *arcadeRegion[] = { "Japan", "USA", "Export", "Korea" };
 	const char **region = settings.platform.isArcade() ? arcadeRegion : consoleRegion;
-	OptionComboBox("Region", config::Region, region, std::size(consoleRegion),
+	OptionComboBox(tr("Region"), config::Region, region, std::size(consoleRegion),
 				"BIOS region");
 
 	const char *cable[] = { "VGA", "RGB Component", "TV Composite" };
@@ -209,7 +210,7 @@ void gui_settings_general()
 		const char *value = config::Cable == 0 ? cable[0]
 				: config::Cable > 0 && config::Cable <= (int)std::size(cable) ? cable[config::Cable - 1]
 				: "?";
-		if (ImGui::BeginCombo("Cable", value, ImGuiComboFlags_None))
+		if (ImGui::BeginCombo(tr("Cable"), value, ImGuiComboFlags_None))
 		{
 			for (int i = 0; i < IM_ARRAYSIZE(cable); i++)
 			{
@@ -232,7 +233,7 @@ void gui_settings_general()
     				* (config::ContentPath.get().size() + 1);
 
     ImVec2 childSize;
-    if (beginFrame("Content Location", size, &childSize))
+    if (beginFrame(tr("Content Location"), size, &childSize))
     {
     	int to_delete = -1;
         for (u32 i = 0; i < config::ContentPath.get().size(); i++)
@@ -249,11 +250,11 @@ void gui_settings_general()
         }
 
         ImguiStyleVar _(ImGuiStyleVar_FramePadding, ScaledVec2(24, 3));
-        const bool addContent = ImGui::Button("Add##ContentLocation");
+        const bool addContent = ImGui::Button(tr("Add##ContentLocation"));
         addContentPath(addContent);
         ImGui::SameLine();
 
-        if (ImGui::Button("Rescan Content"))
+        if (ImGui::Button(tr("Rescan Content")))
 			scanner.refresh();
 
 		endFrame();
@@ -271,7 +272,7 @@ void gui_settings_general()
     ImGui::Spacing();
 
 #if defined(__linux__) && !defined(__ANDROID__)
-    if (beginFrame("Data Folder", size, &childSize))
+    if (beginFrame(tr("Data Folder"), size, &childSize))
     {
     	float w = childSize.x - ImGui::GetStyle().FramePadding.x;
     	std::string s = middleEllipsis(get_writable_data_path(""), w);
@@ -284,7 +285,7 @@ void gui_settings_general()
 #if defined(__ANDROID__) || defined(TARGET_MAC)
     size.y += ImGui::GetTextLineHeightWithSpacing();
 #endif
-    if (beginFrame("Home Folder", size, &childSize))
+    if (beginFrame(tr("Home Folder"), size, &childSize))
     {
     	float w = childSize.x - ImGui::GetStyle().FramePadding.x;
     	std::string s = middleEllipsis(get_writable_config_path(""), w);
@@ -317,16 +318,16 @@ void gui_settings_general()
 #else // TARGET_IPHONE
     {
     	ImguiStyleVar _(ImGuiStyleVar_FramePadding, ScaledVec2(24, 3));
-		if (ImGui::Button("Rescan Content"))
+		if (ImGui::Button(tr("Rescan Content")))
 			scanner.refresh();
     }
 #endif
     ImGui::Spacing();
-	OptionCheckbox("Box Art Game List", config::BoxartDisplayMode,
+	OptionCheckbox(tr("Box Art Game List"), config::BoxartDisplayMode,
 			"Display game cover art in the game list.");
-	OptionCheckbox("Fetch Box Art", config::FetchBoxart,
+	OptionCheckbox(tr("Fetch Box Art"), config::FetchBoxart,
 			"Fetch cover images from TheGamesDB.net.");
-	if (OptionSlider("UI Scaling", config::UIScaling, 50, 200, "Adjust the size of UI elements and fonts.", "%d%%"))
+	if (OptionSlider(tr("UI Scaling"), config::UIScaling, 50, 200, "Adjust the size of UI elements and fonts.", "%d%%"))
 		uiUserScaleUpdated = true;
 	if (uiUserScaleUpdated)
 	{
@@ -339,37 +340,37 @@ void gui_settings_general()
 
 	const char *themes[] = { "Dark", "Light", "Dreamcast", "High Contrast", "Nintendo", "Aqua Chill" };
 	int previousUITheme = config::UITheme;
-	OptionComboBox("UI Theme", config::UITheme, themes, std::size(themes),
+	OptionComboBox(tr("UI Theme"), config::UITheme, themes, std::size(themes),
 			"Select the UI color theme.");
 	// Auto-apply theme when selection changes
 	if (previousUITheme != config::UITheme) {
 		applyCurrentTheme();
 	}
 
-	if (OptionCheckbox("Hide Legacy Naomi Roms", config::HideLegacyNaomiRoms,
+	if (OptionCheckbox(tr("Hide Legacy Naomi Roms"), config::HideLegacyNaomiRoms,
 			"Hide .bin, .dat and .lst files from the content browser"))
 		scanner.refresh();
 #ifdef __ANDROID__
-	OptionCheckbox("Use SAF File Picker", config::UseSafFilePicker,
+	OptionCheckbox(tr("Use SAF File Picker"), config::UseSafFilePicker,
 			"Use Android Storage Access Framework file picker to select folders and files. Ignored on Android 10 and later.");
 #endif
 
-	ImGui::Text("Automatic State:");
-	OptionCheckbox("Load", config::AutoLoadState,
+	ImGui::Text(tr("Automatic State:"));
+	OptionCheckbox(tr("Load"), config::AutoLoadState,
 			"Load the last saved state of the game when starting");
 	ImGui::SameLine();
-	OptionCheckbox("Save", config::AutoSaveState,
+	OptionCheckbox(tr("Save"), config::AutoSaveState,
 			"Save the state of the game when stopping");
-	OptionCheckbox("Naomi Free Play", config::ForceFreePlay, "Configure Naomi games in Free Play mode.");
+	OptionCheckbox(tr("Naomi Free Play"), config::ForceFreePlay, "Configure Naomi games in Free Play mode.");
 #if USE_DISCORD
-	OptionCheckbox("Discord Presence", config::DiscordPresence, "Show which game you are playing on Discord");
+	OptionCheckbox(tr("Discord Presence"), config::DiscordPresence, "Show which game you are playing on Discord");
 #endif
 #ifdef USE_RACHIEVEMENTS
-	OptionCheckbox("Enable RetroAchievements", config::EnableAchievements, "Track your game achievements using RetroAchievements.org");
+	OptionCheckbox(tr("Enable RetroAchievements"), config::EnableAchievements, "Track your game achievements using RetroAchievements.org");
 	{
 		DisabledScope _(!config::EnableAchievements);
 		ImGui::Indent();
-		OptionCheckbox("Hardcore Mode", config::AchievementsHardcoreMode,
+		OptionCheckbox(tr("Hardcore Mode"), config::AchievementsHardcoreMode,
 				"Enable RetroAchievements hardcore mode. Using cheats and loading a state are not allowed in this mode.");
 		InputText("Username", &config::AchievementsUserName.get(),
 				achievements::isLoggedOn() ? ImGuiInputTextFlags_ReadOnly : ImGuiInputTextFlags_None);
@@ -379,7 +380,7 @@ void gui_settings_general()
 			achievements::init();
 			if (achievements::isLoggedOn())
 			{
-				ImGui::Text("Authentication successful");
+				ImGui::Text(tr("Authentication successful"));
 				if (futureLogin.valid())
 					futureLogin.get();
 				if (ImGui::Button("Logout", ScaledVec2(100, 0)))
@@ -392,7 +393,7 @@ void gui_settings_general()
 				if (futureLogin.valid())
 				{
 					if (futureLogin.wait_for(std::chrono::seconds::zero()) == std::future_status::timeout) {
-						ImGui::Text("Authenticating...");
+						ImGui::Text(tr("Authenticating..."));
 					}
 					else
 					{
@@ -420,44 +421,44 @@ void gui_settings_general()
 // Custom Paths section - hidden on Android and iOS
 #if !defined(TARGET_IPHONE)
     ImGui::Spacing();
-    header("Custom Paths");
+    header(tr("Custom Paths"));
 
     managePathList("BIOS Folders", config::BiosPath.get(),
         "Folders containing BIOS/Flash files (e.g. dc_boot.bin, dc_flash.bin) and arcade BIOS");
     ImGui::Spacing();
 
 #if !defined(__ANDROID__)
-    manageSinglePath("VMU Folder", config::VMUPath,
+    manageSinglePath(tr("VMU Folder"), config::VMUPath,
         "Folder where VMU (.bin) saves are stored");
     ImGui::Spacing();
 
-    managePathList("Savestate Folders", config::SavestatePath.get(),
+    managePathList(tr("Savestate Folders"), config::SavestatePath.get(),
         "Folders for save states. First path is used for new states; all are searched when loading");
     ImGui::Spacing();
 
-    manageSinglePath("Game Save Folder", config::SavePath,
+    manageSinglePath(tr("Game Save Folder"), config::SavePath,
         "Folder for game save data (e.g. arcade NVRAM)");
     ImGui::Spacing();
 #endif
 
-    managePathList("Texture Pack Folders", config::TexturePath.get(),
+    managePathList(tr("Texture Pack Folders"), config::TexturePath.get(),
         "Folders containing textures/<gameId> or <gameId> under a textures subfolder");
     ImGui::Spacing();
 
 #if !defined(__ANDROID__)
-    manageSinglePath("Texture Dump Folder", config::TextureDumpPath, 
+    manageSinglePath(tr("Texture Dump Folder"), config::TextureDumpPath, 
         "Folder where texture dumps are saved. Game-specific subfolders will be created automatically");
     ImGui::Spacing();
     
-    manageSinglePath("Box Art Folder", config::BoxartPath,
+    manageSinglePath(tr("Box Art Folder"), config::BoxartPath,
         "Folder containing box art images (png/jpg). If empty, Flycast will use the default Home Folder/boxart for downloads and generated art");
     ImGui::Spacing();
 
-    managePathList("Controller Mapping Folders", config::MappingsPath.get(),
+    managePathList(tr("Controller Mapping Folders"), config::MappingsPath.get(),
         "Folders containing controller mapping files (.cfg). The emulator also looks in Home Folder/mappings. Per-game mappings are suffixed with _<gameId>.cfg");
     ImGui::Spacing();
 
-    managePathList("Cheat Folders", config::CheatPath.get(),
+    managePathList(tr("Cheat Folders"), config::CheatPath.get(),
         "Folders containing cheat files (.cht/.txt) named with the game ID. Flycast will auto-load matching files if present");
     ImGui::Spacing();
 #endif  // !ANDROID
diff --git a/core/ui/settings_network.cpp b/core/ui/settings_network.cpp
index c1137dc87..e3612dc06 100644
--- a/core/ui/settings_network.cpp
+++ b/core/ui/settings_network.cpp
@@ -17,6 +17,7 @@
     along with Flycast.  If not, see <https://www.gnu.org/licenses/>.
  */
 #include "settings.h"
+#include "i18n.h"
 #include "gui.h"
 #include "network/ggpo.h"
 #include "network/ice.h"
@@ -25,7 +26,7 @@
 void gui_settings_network()
 {
 	ImGuiStyle& style = ImGui::GetStyle();
-	header("Network Type");
+	header(tr("Network Type"));
 	{
 		DisabledScope scope(game_started);
 
@@ -37,17 +38,17 @@ void gui_settings_network()
 		else if (config::BattleCableEnable)
 			netType = 3;
 		ImGui::Columns(4, "networkType", false);
-		ImGui::RadioButton("Disabled##network", &netType, 0);
+		ImGui::RadioButton(tr("Disabled##network"), &netType, 0);
 		ImGui::NextColumn();
-		ImGui::RadioButton("GGPO", &netType, 1);
+		ImGui::RadioButton(tr("GGPO"), &netType, 1);
 		ImGui::SameLine(0, style.ItemInnerSpacing.x);
 		ShowHelpMarker("Enable networking using GGPO");
 		ImGui::NextColumn();
-		ImGui::RadioButton("Naomi", &netType, 2);
+		ImGui::RadioButton(tr("Naomi"), &netType, 2);
 		ImGui::SameLine(0, style.ItemInnerSpacing.x);
 		ShowHelpMarker("Enable networking for supported Naomi and Atomiswave games");
 		ImGui::NextColumn();
-		ImGui::RadioButton("Battle Cable", &netType, 3);
+		ImGui::RadioButton(tr("Battle Cable"), &netType, 3);
 		ImGui::SameLine(0, style.ItemInnerSpacing.x);
 		ShowHelpMarker("Emulate the Taisen (Battle) null modem cable for games that support it");
 		ImGui::Columns(1, nullptr, false);
@@ -69,31 +70,31 @@ void gui_settings_network()
 	}
 	if (config::GGPOEnable || config::NetworkEnable || config::BattleCableEnable) {
 		ImGui::Spacing();
-		header("Configuration");
+		header(tr("Configuration"));
 	}
 	{
 		if (config::GGPOEnable)
 		{
 			config::NetworkEnable = false;
-			OptionCheckbox("Play as Player 1", config::ActAsServer,
+			OptionCheckbox(tr("Play as Player 1"), config::ActAsServer,
 					"Deselect to play as player 2");
 			InputText("Peer", &config::NetworkServer.get(), ImGuiInputTextFlags_CharsNoBlank | ImGuiInputTextFlags_CallbackCharFilter, dnsCharFilter);
 			ImGui::SameLine();
 			ShowHelpMarker("Your peer IP address and optional port");
-			OptionSlider("Frame Delay", config::GGPODelay, 0, 20,
+			OptionSlider(tr("Frame Delay"), config::GGPODelay, 0, 20,
 				"Sets Frame Delay, advisable for sessions with ping >100 ms");
 
-			ImGui::Text("Left Thumbstick:");
-			OptionRadioButton<int>("Disabled##analogaxis", config::GGPOAnalogAxes, 0, "Left thumbstick not used");
+			ImGui::Text(tr("Left Thumbstick:"));
+			OptionRadioButton<int>(tr("Disabled##analogaxis"), config::GGPOAnalogAxes, 0, tr("Left thumbstick not used"));
 			ImGui::SameLine();
-			OptionRadioButton<int>("Horizontal", config::GGPOAnalogAxes, 1, "Use the left thumbstick horizontal axis only");
+			OptionRadioButton<int>(tr("Horizontal"), config::GGPOAnalogAxes, 1, tr("Use the left thumbstick horizontal axis only"));
 			ImGui::SameLine();
-			OptionRadioButton<int>("Full", config::GGPOAnalogAxes, 2, "Use the left thumbstick horizontal and vertical axes");
+			OptionRadioButton<int>(tr("Full"), config::GGPOAnalogAxes, 2, tr("Use the left thumbstick horizontal and vertical axes"));
 
-			OptionCheckbox("Enable Chat", config::GGPOChat, "Open the chat window when a chat message is received");
+			OptionCheckbox(tr("Enable Chat"), config::GGPOChat, "Open the chat window when a chat message is received");
 			if (config::GGPOChat)
 			{
-				OptionCheckbox("Enable Chat Window Timeout", config::GGPOChatTimeoutToggle, "Automatically close chat window after 20 seconds");
+				OptionCheckbox(tr("Enable Chat Window Timeout"), config::GGPOChatTimeoutToggle, "Automatically close chat window after 20 seconds");
 				if (config::GGPOChatTimeoutToggle)
 				{
 					char chatTimeout[256];
@@ -104,12 +105,12 @@ void gui_settings_network()
 					config::GGPOChatTimeout.set(atoi(chatTimeout));
 				}
 			}
-			OptionCheckbox("Network Statistics", config::NetworkStats,
+			OptionCheckbox(tr("Network Statistics"), config::NetworkStats,
 					"Display network statistics on screen");
 		}
 		else if (config::NetworkEnable)
 		{
-			OptionCheckbox("Act as Server", config::ActAsServer,
+			OptionCheckbox(tr("Act as Server"), config::ActAsServer,
 					"Create a local server for Naomi network games");
 			if (!config::ActAsServer)
 			{
@@ -138,11 +139,11 @@ void gui_settings_network()
 					ImGui::SameLine();
 					ShowHelpMarker("Choose a unique word or number and share it with your opponent.");
 					if (state == ice::Offline) {
-						if (ImGui::Button("Connect") && !matchCode.empty())
+						if (ImGui::Button(tr("Connect")) && !matchCode.empty())
 							ice::init(matchCode, true);
 					}
 					else {
-						if (ImGui::Button("Disconnect"))
+						if (ImGui::Button(tr("Disconnect")))
 							try { ice::term(); } catch (...) {}
 					}
 					std::string status;
@@ -164,7 +165,7 @@ void gui_settings_network()
 						break;
 					}
 					ImGui::TextDisabled("%s", status.c_str());
-					OptionCheckbox("Network Statistics", config::NetworkStats,
+					OptionCheckbox(tr("Network Statistics"), config::NetworkStats,
 							"Display network statistics on screen");
 					ImGui::EndTabItem();
 				}
@@ -186,23 +187,23 @@ void gui_settings_network()
 				ImGui::EndTabBar();
 		    }
 #endif
-			OptionCheckbox("Act as Master", config::ActAsServer,
+			OptionCheckbox(tr("Act as Master"), config::ActAsServer,
 					"Only used for Maximum Speed. One of the peer must be master.");
 		}
 	}
 	ImGui::Spacing();
-	header("Network Options");
+	header(tr("Network Options"));
 	{
-		OptionCheckbox("Enable UPnP", config::EnableUPnP, "Automatically configure your network router for netplay");
-		OptionCheckbox("Broadcast Digital Outputs", config::NetworkOutput, "Broadcast digital outputs and force-feedback state on TCP port 8000. "
+		OptionCheckbox(tr("Enable UPnP"), config::EnableUPnP, "Automatically configure your network router for netplay");
+		OptionCheckbox(tr("Broadcast Digital Outputs"), config::NetworkOutput, "Broadcast digital outputs and force-feedback state on TCP port 8000. "
 				"Compatible with the \"-output network\" MAME option. Arcade games only.");
 		{
 			DisabledScope scope(game_started);
 
-			OptionCheckbox("Broadband Adapter Emulation", config::EmulateBBA,
+			OptionCheckbox(tr("Broadband Adapter Emulation"), config::EmulateBBA,
 					"Emulate the Ethernet Broadband Adapter (BBA) instead of the Modem");
 		}
-		OptionCheckbox("Use DCNet", config::UseDCNet, "Use the DCNet cloud service for Dreamcast Internet access.");
+		OptionCheckbox(tr("Use DCNet"), config::UseDCNet, "Use the DCNet cloud service for Dreamcast Internet access.");
 
 		std::string& ispUsername = config::ISPUsername;
 		InputText("ISP User Name", &ispUsername, ImGuiInputTextFlags_CharsNoBlank | ImGuiInputTextFlags_CallbackCharFilter,
@@ -223,12 +224,12 @@ void gui_settings_network()
 	}
 #ifdef NAOMI_MULTIBOARD
 	ImGui::Spacing();
-	header("Multiboard Screens");
+	header(tr("Multiboard Screens"));
 	{
 		//OptionRadioButton<int>("Disabled##multiboard", config::MultiboardSlaves, 0, "Multiboard disabled (when optional)");
-		OptionRadioButton<int>("1 (Twin)", config::MultiboardSlaves, 1, "One screen configuration (F355 Twin)");
+		OptionRadioButton<int>(tr("1 (Twin)"), config::MultiboardSlaves, 1, tr("One screen configuration (F355 Twin)"));
 		ImGui::SameLine();
-		OptionRadioButton<int>("3 (Deluxe)", config::MultiboardSlaves, 2, "Three screens configuration");
+		OptionRadioButton<int>(tr("3 (Deluxe)"), config::MultiboardSlaves, 2, tr("Three screens configuration"));
 	}
 #endif
 }
diff --git a/core/ui/settings_video.cpp b/core/ui/settings_video.cpp
index 7ea089b02..3c720dfa3 100644
--- a/core/ui/settings_video.cpp
+++ b/core/ui/settings_video.cpp
@@ -17,6 +17,7 @@
     along with Flycast.  If not, see <https://www.gnu.org/licenses/>.
  */
 #include "settings.h"
+#include "i18n.h"
 #include "gui.h"
 #include "wsi/context.h"
 
@@ -75,7 +76,7 @@ void gui_settings_video()
     float innerSpacing = ImGui::GetStyle().ItemInnerSpacing.x;
 	if (apiCount > 1)
 	{
-		header("Graphics API");
+		header(tr("Graphics API"));
 		{
 			ImGui::Columns(apiCount, "renderApi", false);
 #ifdef USE_OPENGL
@@ -106,7 +107,7 @@ void gui_settings_video()
 			ImGui::Columns(1, nullptr, false);
     	}
     }
-    header("Transparent Sorting");
+    header(tr("Transparent Sorting"));
     {
 		const bool has_per_pixel = GraphicsContext::Instance()->hasPerPixel();
     	int renderer = perPixel ? 2 : config::PerStripSorting ? 1 : 0;
@@ -143,7 +144,7 @@ void gui_settings_video()
     }
 	ImGui::Spacing();
 
-    header("Rendering Options");
+    header(tr("Rendering Options"));
     {
         const std::array<float, 13> scalings{ 0.5f, 1.f, 1.5f, 2.f, 2.5f, 3.f, 4.f, 4.5f, 5.f, 6.f, 7.f, 8.f, 9.f };
         const std::array<std::string, 13> scalingsText{ "Half", "Native", "x1.5", "x2", "x2.5", "x3", "x4", "x4.5", "x5", "x6", "x7", "x8", "x9" };
@@ -191,64 +192,64 @@ void gui_settings_video()
         }
         ImGui::SameLine(0, innerSpacing);
 
-        ImGui::Text("Internal Resolution");
+        ImGui::Text(tr("Internal Resolution"));
         ImGui::SameLine();
         ShowHelpMarker("Internal render resolution. Higher is better, but more demanding on the GPU. Values higher than your display resolution (but no more than double your display resolution) can be used for supersampling, which provides high-quality antialiasing without reducing sharpness.");
-		OptionCheckbox("Integer Scaling", config::IntegerScale, "Scales the output by the maximum integer multiple allowed by the display resolution.");
-		OptionCheckbox("Linear Interpolation", config::LinearInterpolation, "Scales the output with linear interpolation. Will use nearest neighbor interpolation otherwise. Disable with integer scaling.");
+		OptionCheckbox(tr("Integer Scaling"), config::IntegerScale, "Scales the output by the maximum integer multiple allowed by the display resolution.");
+		OptionCheckbox(tr("Linear Interpolation"), config::LinearInterpolation, "Scales the output with linear interpolation. Will use nearest neighbor interpolation otherwise. Disable with integer scaling.");
 #ifndef TARGET_IPHONE
-    	OptionCheckbox("VSync", config::VSync, "Synchronizes the frame rate with the screen refresh rate. Recommended");
+    	OptionCheckbox(tr("VSync"), config::VSync, "Synchronizes the frame rate with the screen refresh rate. Recommended");
     	if (isVulkan(config::RendererType))
     	{
 	    	ImGui::Indent();
 			{
 				DisabledScope scope(!config::VSync);
 
-				OptionCheckbox("Duplicate frames", config::DupeFrames, "Duplicate frames on high refresh rate monitors (120 Hz and higher)");
+				OptionCheckbox(tr("Duplicate frames"), config::DupeFrames, "Duplicate frames on high refresh rate monitors (120 Hz and higher)");
 	    	}
 	    	ImGui::Unindent();
     	}
 #endif
-    	OptionCheckbox("Show VMU In-game", config::FloatVMUs, "Show the VMU LCD screens while in-game");
-    	OptionCheckbox("Full Framebuffer Emulation", config::EmulateFramebuffer,
+    	OptionCheckbox(tr("Show VMU In-game"), config::FloatVMUs, "Show the VMU LCD screens while in-game");
+    	OptionCheckbox(tr("Full Framebuffer Emulation"), config::EmulateFramebuffer,
     			"Fully accurate VRAM framebuffer emulation. Helps games that directly access the framebuffer for special effects. "
     			"Very slow and incompatible with upscaling and wide screen.");
 		{
 			DisabledScope scope(game_started);
-			OptionCheckbox("Load Custom Textures", config::CustomTextures,
+			OptionCheckbox(tr("Load Custom Textures"), config::CustomTextures,
 						   "Load custom/high-res textures from data/textures/<game id>");
 			ImGui::Indent();
 			{
 				DisabledScope scope(!config::CustomTextures.get());
-				OptionCheckbox("Preload Custom Textures", config::PreloadCustomTextures,
+				OptionCheckbox(tr("Preload Custom Textures"), config::PreloadCustomTextures,
 							   "Preload custom textures at game start. May improve performance but increases memory usage");
 			}
 			ImGui::Unindent();
 		}
     }
 	ImGui::Spacing();
-    header("Aspect Ratio");
+    header(tr("Aspect Ratio"));
     {
-    	OptionCheckbox("Widescreen", config::Widescreen,
+    	OptionCheckbox(tr("Widescreen"), config::Widescreen,
     			"Draw geometry outside of the normal 4:3 aspect ratio. May produce graphical glitches in the revealed areas.\nAspect Fit and shows the full 16:9 content.");
 		{
 			DisabledScope scope(!config::Widescreen || config::IntegerScale);
 
 			ImGui::Indent();
-			OptionCheckbox("Super Widescreen", config::SuperWidescreen,
+			OptionCheckbox(tr("Super Widescreen"), config::SuperWidescreen,
 					"Use the full width of the screen or window when its aspect ratio is greater than 16:9.\nAspect Fill and remove black bars. Not compatible with integer scaling.");
 			ImGui::Unindent();
     	}
-    	OptionCheckbox("Widescreen Game Cheats", config::WidescreenGameHacks,
+    	OptionCheckbox(tr("Widescreen Game Cheats"), config::WidescreenGameHacks,
     			"Modify the game so that it displays in 16:9 anamorphic format and use horizontal screen stretching. Only some games are supported.");
-    	OptionSlider("Horizontal Stretching", config::ScreenStretching, 100, 250,
+    	OptionSlider(tr("Horizontal Stretching"), config::ScreenStretching, 100, 250,
     			"Stretch the screen horizontally", "%d%%");
-    	OptionCheckbox("Rotate Screen 90°", config::Rotate90, "Rotate the screen 90° counterclockwise");
+    	OptionCheckbox(tr("Rotate Screen 90°"), config::Rotate90, "Rotate the screen 90° counterclockwise");
     }
 	if (perPixel)
 	{
 		ImGui::Spacing();
-		header("Per Pixel Settings");
+		header(tr("Per Pixel Settings"));
 
 		const std::array<int64_t, 4> bufSizes{ 512_MB, 1_GB, 2_GB, 4_GB };
 		const std::array<std::string, 4> bufSizesText{ "512 MB", "1 GB", "2 GB", "4 GB" };
@@ -289,41 +290,41 @@ void gui_settings_video()
 		}
 		ImGui::SameLine(0, innerSpacing);
 
-        ImGui::Text("Pixel Buffer Size");
+        ImGui::Text(tr("Pixel Buffer Size"));
         ImGui::SameLine();
         ShowHelpMarker("The size of the pixel buffer. May need to be increased when upscaling by a large factor.");
 
-        OptionSlider("Maximum Layers", config::PerPixelLayers, 8, 128,
+        OptionSlider(tr("Maximum Layers"), config::PerPixelLayers, 8, 128,
         		"Maximum number of transparent layers. May need to be increased for some complex scenes. Decreasing it may improve performance.");
 	}
 	ImGui::Spacing();
-    header("Performance");
+    header(tr("Performance"));
     {
-    	ImGui::Text("Automatic Frame Skipping:");
+    	ImGui::Text(tr("Automatic Frame Skipping:"));
     	ImGui::Columns(3, "autoskip", false);
-    	OptionRadioButton("Disabled", config::AutoSkipFrame, 0, "No frame skipping");
+    	OptionRadioButton(tr("Disabled"), config::AutoSkipFrame, 0, "No frame skipping");
     	ImGui::NextColumn();
-    	OptionRadioButton("Normal", config::AutoSkipFrame, 1, "Skip a frame when the GPU and CPU are both running slow");
+    	OptionRadioButton(tr("Normal"), config::AutoSkipFrame, 1, "Skip a frame when the GPU and CPU are both running slow");
     	ImGui::NextColumn();
-    	OptionRadioButton("Maximum", config::AutoSkipFrame, 2, "Skip a frame when the GPU is running slow");
+    	OptionRadioButton(tr("Maximum"), config::AutoSkipFrame, 2, "Skip a frame when the GPU is running slow");
     	ImGui::Columns(1, nullptr, false);
 
     	OptionArrowButtons("Frame Skipping", config::SkipFrame, 0, 6,
     			"Number of frames to skip between two actually rendered frames");
-    	OptionCheckbox("Shadows", config::ModifierVolumes,
+    	OptionCheckbox(tr("Shadows"), config::ModifierVolumes,
     			"Enable modifier volumes, usually used for shadows");
-    	OptionCheckbox("Fog", config::Fog, "Enable fog effects");
+    	OptionCheckbox(tr("Fog"), config::Fog, "Enable fog effects");
     }
     ImGui::Spacing();
-	header("Advanced");
+	header(tr("Advanced"));
     {
-    	OptionCheckbox("Delay Frame Swapping", config::DelayFrameSwapping,
+    	OptionCheckbox(tr("Delay Frame Swapping"), config::DelayFrameSwapping,
     			"Useful to avoid flashing screen or glitchy videos. Not recommended on slow platforms");
-    	OptionCheckbox("Fix Upscale Bleeding Edge", config::FixUpscaleBleedingEdge,
+    	OptionCheckbox(tr("Fix Upscale Bleeding Edge"), config::FixUpscaleBleedingEdge,
     			"Helps with texture bleeding case when upscaling. Disabling it can help if pixels are warping when upscaling in 2D games (MVC2, CVS, KOF, etc.)");
-    	OptionCheckbox("Native Depth Interpolation", config::NativeDepthInterpolation,
+    	OptionCheckbox(tr("Native Depth Interpolation"), config::NativeDepthInterpolation,
     			"Helps with texture corruption and depth issues on AMD GPUs. Can also help Intel GPUs in some cases.");
-    	OptionCheckbox("Copy Rendered Textures to VRAM", config::RenderToTextureBuffer,
+    	OptionCheckbox(tr("Copy Rendered Textures to VRAM"), config::RenderToTextureBuffer,
     			"Copy rendered-to textures back to VRAM. Slower but accurate");
 		const std::array<int, 5> aniso{ 1, 2, 4, 8, 16 };
         const std::array<std::string, 5> anisoText{ "Disabled", "2x", "4x", "8x", "16x" };
@@ -363,38 +364,38 @@ void gui_settings_video()
         }
         ImGui::SameLine(0, innerSpacing);
 
-        ImGui::Text("Anisotropic Filtering");
+        ImGui::Text(tr("Anisotropic Filtering"));
         ImGui::SameLine();
         ShowHelpMarker("Higher values make textures viewed at oblique angles look sharper, but are more demanding on the GPU. This option only has a visible impact on mipmapped textures.");
 
-    	ImGui::Text("Texture Filtering:");
+    	ImGui::Text(tr("Texture Filtering:"));
     	ImGui::Columns(3, "textureFiltering", false);
-    	OptionRadioButton("Default", config::TextureFiltering, 0, "Use the game's default texture filtering");
+    	OptionRadioButton(tr("Default"), config::TextureFiltering, 0, "Use the game's default texture filtering");
     	ImGui::NextColumn();
-    	OptionRadioButton("Force Nearest-Neighbor", config::TextureFiltering, 1, "Force nearest-neighbor filtering for all textures. Crisper appearance, but may cause various rendering issues. This option usually does not affect performance.");
+    	OptionRadioButton(tr("Force Nearest-Neighbor"), config::TextureFiltering, 1, "Force nearest-neighbor filtering for all textures. Crisper appearance, but may cause various rendering issues. This option usually does not affect performance.");
     	ImGui::NextColumn();
-    	OptionRadioButton("Force Linear", config::TextureFiltering, 2, "Force linear filtering for all textures. Smoother appearance, but may cause various rendering issues. This option usually does not affect performance.");
+    	OptionRadioButton(tr("Force Linear"), config::TextureFiltering, 2, "Force linear filtering for all textures. Smoother appearance, but may cause various rendering issues. This option usually does not affect performance.");
     	ImGui::Columns(1, nullptr, false);
 
-    	OptionCheckbox("Show FPS Counter", config::ShowFPS, "Show on-screen frame/sec counter");
+    	OptionCheckbox(tr("Show FPS Counter"), config::ShowFPS, "Show on-screen frame/sec counter");
     }
 #ifdef VIDEO_ROUTING
 	ImGui::Spacing();
 #ifdef __APPLE__
-	header("Video Routing (Syphon)");
+	header(tr("Video Routing (Syphon)"));
 #elif defined(_WIN32)
-	((renderApi == 0) || (renderApi == 3)) ? header("Video Routing (Spout)") : header("Video Routing (Only available with OpenGL or DirectX 11)");
+	((renderApi == 0) || (renderApi == 3)) ? header(tr("Video Routing (Spout)")) : header(tr("Video Routing (Only available with OpenGL or DirectX 11)"));
 #endif
 	{
 #ifdef _WIN32
 		DisabledScope scope(!((renderApi == 0) || (renderApi == 3)));
 #endif
-		OptionCheckbox("Send video content to another program", config::VideoRouting,
+		OptionCheckbox(tr("Send video content to another program"), config::VideoRouting,
 			"e.g. Route GPU texture to OBS Studio directly instead of using CPU intensive Display/Window Capture");
 
 		{
 			DisabledScope scope(!config::VideoRouting);
-			OptionCheckbox("Scale down before sending", config::VideoRoutingScale, "Could increase performance when sharing a smaller texture, YMMV");
+			OptionCheckbox(tr("Scale down before sending"), config::VideoRoutingScale, "Could increase performance when sharing a smaller texture, YMMV");
 			{
 				DisabledScope scope(!config::VideoRoutingScale);
 				static int vres = config::VideoRoutingVRes;
@@ -403,7 +404,7 @@ void gui_settings_video()
 					config::VideoRoutingVRes = vres;
 				}
 			}
-			ImGui::Text("Output texture size: %d x %d", config::VideoRoutingScale ? config::VideoRoutingVRes * settings.display.width / settings.display.height : settings.display.width, config::VideoRoutingScale ? config::VideoRoutingVRes : settings.display.height);
+			ImGui::Text(tr("Output texture size: %d x %d"), config::VideoRoutingScale ? config::VideoRoutingVRes * settings.display.width / settings.display.height : settings.display.width, config::VideoRoutingScale ? config::VideoRoutingVRes : settings.display.height);
 		}
 	}
 #endif
diff --git a/core/ui/i18n.h b/core/ui/i18n.h
new file mode 100755
index 000000000..6d8ca62f6
--- /dev/null
+++ b/core/ui/i18n.h
@@ -0,0 +1,604 @@
+#pragma once
+#include <cstring>
+#include <unordered_map>
+
+struct LangEntry {
+    const char* en;
+    const char* zh;
+};
+
+static const std::unordered_map<std::string, LangEntry> translations = {
+    // Tabs
+    {"General", {"General", "通用设置"}},
+    {"System", {"System", "系统"}},
+    {"Video", {"Video", "视频"}},
+    {"Audio", {"Audio", "音频"}},
+    {"Controls", {"Controls", "控制"}},
+    {"Network", {"Network", "网络"}},
+    {"Advanced", {"Advanced", "高级"}},
+    {"About", {"About", "关于"}},
+    
+    // General
+    {"Region", {"Region", "区域"}},
+    {"Language", {"Language", "语言"}},
+    {"Broadcast", {"Broadcast", "广播制式"}},
+    {"Cable", {"Cable", "线缆类型"}},
+    {"Box Art Game List", {"Box Art Game List", "封面图游戏列表"}},
+    {"Display game cover art in the game list.", {"Display game cover art in the game list.", "在游戏列表中显示封面图"}},
+    {"Fetch Box Art", {"Fetch Box Art", "获取封面图"}},
+    {"Fetch cover images from TheGamesDB.net.", {"Fetch cover images from TheGamesDB.net.", "从 TheGamesDB.net 获取封面图"}},
+    {"Hide Legacy Naomi Roms", {"Hide Legacy Naomi Roms", "隐藏旧版 Naomi ROM"}},
+    {"Hide .bin, .dat and .lst files from the content browser", {"Hide .bin, .dat and .lst files from the content browser", "在内容浏览器中隐藏 .bin、.dat 和 .lst 文件"}},
+    {"Use SAF File Picker", {"Use SAF File Picker", "使用 SAF 文件选择器"}},
+    {"Use Android Storage Access Framework file picker to select folders and files. Ignored on Android 10 and later.", {"Use Android Storage Access Framework file picker to select folders and files. Ignored on Android 10 and later.", "使用 Android SAF 文件选择器选择文件夹和文件。Android 10 及以上版本忽略此选项"}},
+    {"Per-Game VMU", {"Per-Game VMU", "每游戏独立 VMU"}},
+    {"UI Scaling", {"UI Scaling", "界面缩放"}},
+    {"Adjust the size of UI elements and fonts.", {"Adjust the size of UI elements and fonts.", "调整界面元素和字体大小"}},
+    {"UI Theme", {"UI Theme", "界面主题"}},
+    {"Select the UI color theme.", {"Select the UI color theme.", "选择界面颜色主题"}},
+    {"Automatic State:", {"Automatic State:", "自动存档："}},
+    {"Load", {"Load", "读取"}},
+    {"Save", {"Save", "保存"}},
+    {"Load the last saved state of the game when starting", {"Load the last saved state of the game when starting", "启动时加载游戏的最后存档"}},
+    {"Save the state of the game when stopping", {"Save the state of the game when stopping", "停止时保存游戏状态"}},
+    {"Naomi Free Play", {"Naomi Free Play", "Naomi 免费游戏"}},
+    {"Configure Naomi games in Free Play mode.", {"Configure Naomi games in Free Play mode.", "配置 Naomi 游戏为免费模式"}},
+    {"Discord Presence", {"Discord Presence", "Discord 状态显示"}},
+    {"Show which game you are playing on Discord", {"Show which game you are playing on Discord", "在 Discord 上显示你正在玩的游戏"}},
+    {"Enable RetroAchievements", {"Enable RetroAchievements", "启用 RetroAchievements"}},
+    {"Track your game achievements using RetroAchievements.org", {"Track your game achievements using RetroAchievements.org", "使用 RetroAchievements.org 追踪游戏成就"}},
+    {"Hardcore Mode", {"Hardcore Mode", "硬核模式"}},
+    {"Enable RetroAchievements hardcore mode. Using cheats and loading a state are not allowed in this mode.", {"Enable RetroAchievements hardcore mode. Using cheats and loading a state are not allowed in this mode.", "启用 RetroAchievements 硬核模式。此模式下不允许使用金手指和加载存档"}},
+    {"Cheats", {"Cheats", "金手指"}},
+    {"Configuration", {"Configuration", "配置"}},
+    
+    // Video
+    {"Internal Resolution", {"Internal Resolution", "内部分辨率"}},
+    {"Internal render resolution. Higher is better, but more demanding on the GPU. Values higher than your display resolution (but no more than double your display resolution) can be used for supersampling, which provides high-quality antialiasing without reducing sharpness.", {"Internal render resolution. Higher is better, but more demanding on the GPU. Values higher than your display resolution (but no more than double your display resolution) can be used for supersampling, which provides high-quality antialiasing without reducing sharpness.", "内部分辨率。越高越好，但对 GPU 要求更高。可以使用高于显示分辨率（但不超过两倍）的值进行超级采样，在不降低清晰度的情况下提供高质量抗锯齿"}},
+    {"Aspect Ratio", {"Aspect Ratio", "宽高比"}},
+    {"Horizontal Stretching", {"Horizontal Stretching", "水平拉伸"}},
+    {"Modify the game so that it displays in 16:9 anamorphic format and use horizontal screen stretching. Only some games are supported.", {"Modify the game so that it displays in 16:9 anamorphic format and use horizontal screen stretching. Only some games are supported.", "修改游戏以 16:9 变形格式显示并使用水平屏幕拉伸。仅支持部分游戏"}},
+    {"Integer Scaling", {"Integer Scaling", "整数缩放"}},
+    {"Use the full width of the screen or window when its aspect ratio is greater than 16:9.\nAspect Fill and remove black bars. Not compatible with integer scaling.", {"Use the full width of the screen or window when its aspect ratio is greater than 16:9.\nAspect Fill and remove black bars. Not compatible with integer scaling.", "当屏幕宽高比大于 16:9 时使用全屏宽度。填充并移除黑边。与整数缩放不兼容"}},
+    {"Anisotropic Filtering", {"Anisotropic Filtering", "各向异性过滤"}},
+    {"Higher values make textures viewed at oblique angles look sharper, but are more demanding on the GPU. This option only has a visible impact on mipmapped textures.", {"Higher values make textures viewed at oblique angles look sharper, but are more demanding on the GPU. This option only has a visible impact on mipmapped textures.", "更高的值使斜角查看的纹理更清晰，但对 GPU 要求更高。此选项仅对 mipmap 纹理有可见影响"}},
+    {"Texture Filtering:", {"Texture Filtering:", "纹理过滤："}},
+    {"Per Pixel", {"Per Pixel", "逐像素"}},
+    {"Per Strip", {"Per Strip", "逐条带"}},
+    {"Per Triangle", {"Per Triangle", "逐三角形"}},
+    {"Sort transparent polygons per pixel. Slower but accurate", {"Sort transparent polygons per pixel. Slower but accurate", "逐像素排序透明多边形。较慢但精确"}},
+    {"Sort transparent polygons per strip. Faster but may produce graphical glitches", {"Sort transparent polygons per strip. Faster but may produce graphical glitches", "逐条带排序透明多边形。较快但可能产生图形故障"}},
+    {"Sort transparent polygons per triangle. Fast but may produce graphical glitches", {"Sort transparent polygons per triangle. Fast but may produce graphical glitches", "逐三角形排序透明多边形。快但可能产生图形故障"}},
+    {"Transparent Sorting", {"Transparent Sorting", "透明排序"}},
+    {"Frame Delay", {"Frame Delay", "帧延迟"}},
+    {"Frame Skipping", {"Frame Skipping", "跳帧"}},
+    {"No frame skipping", {"No frame skipping", "不跳帧"}},
+    {"Skip a frame when the GPU is running slow", {"Skip a frame when the GPU is running slow", "GPU 运行慢时跳帧"}},
+    {"Skip a frame when the GPU and CPU are both running slow", {"Skip a frame when the GPU and CPU are both running slow", "GPU 和 CPU 都慢时跳帧"}},
+    {"Number of frames to skip between two actually rendered frames", {"Number of frames to skip between two actually rendered frames", "两帧实际渲染之间跳过的帧数"}},
+    {"Automatic Frame Skipping:", {"Automatic Frame Skipping:", "自动跳帧："}},
+    {"Pixel Buffer Size", {"Pixel Buffer Size", "像素缓冲区大小"}},
+    {"The size of the pixel buffer. May need to be increased when upscaling by a large factor.", {"The size of the pixel buffer. May need to be increased when upscaling by a large factor.", "像素缓冲区大小。大幅放大时可能需要增加"}},
+    {"Maximum Layers", {"Maximum Layers", "最大图层数"}},
+    {"Maximum number of transparent layers. May need to be increased for some complex scenes. Decreasing it may improve performance.", {"Maximum number of transparent layers. May need to be increased for some complex scenes. Decreasing it may improve performance.", "最大透明图层数。复杂场景可能需要增加。减少可能提高性能"}},
+    {"Force Linear", {"Force Linear", "强制线性过滤"}},
+    {"Force linear filtering for all textures. Smoother appearance, but may cause various rendering issues. This option usually does not affect performance.", {"Force linear filtering for all textures. Smoother appearance, but may cause various rendering issues. This option usually does not affect performance.", "对所有纹理强制线性过滤。更平滑，但可能导致各种渲染问题。此选项通常不影响性能"}},
+    {"Force Nearest-Neighbor", {"Force Nearest-Neighbor", "强制最近邻过滤"}},
+    {"Force nearest-neighbor filtering for all textures. Crisper appearance, but may cause various rendering issues. This option usually does not affect performance.", {"Force nearest-neighbor filtering for all textures. Crisper appearance, but may cause various rendering issues. This option usually does not affect performance.", "对所有纹理强制最近邻过滤。更清晰，但可能导致各种渲染问题。此选项通常不影响性能"}},
+    {"Use the game's default texture filtering", {"Use the game's default texture filtering", "使用游戏默认纹理过滤"}},
+    {"Native", {"Native", "原生"}},
+    {"Rendering Options", {"Rendering Options", "渲染选项"}},
+    {"Video Routing (Only available with OpenGL or DirectX 11)", {"Video Routing (Only available with OpenGL or DirectX 11)", "视频路由（仅 OpenGL 或 DirectX 11 可用）"}},
+    {"Graphics API", {"Graphics API", "图形 API"}},
+    {"VSync", {"VSync", "垂直同步"}},
+    {"Fullscreen", {"Fullscreen", "全屏"}},
+    {"Crosshair Size", {"Crosshair Size", "准星大小"}},
+    {"Transparency", {"Transparency", "透明度"}},
+    
+    // Audio
+    {"Volume Level", {"Volume Level", "音量级别"}},
+    {"Adjust the emulator's audio level", {"Adjust the emulator's audio level", "调整模拟器音量"}},
+    {"The audio driver to use", {"The audio driver to use", "要使用的音频驱动"}},
+    {"Sets the maximum audio latency. Not supported by all audio drivers.", {"Sets the maximum audio latency. Not supported by all audio drivers.", "设置最大音频延迟。并非所有音频驱动都支持"}},
+    {"Automatically set audio latency. Recommended", {"Automatically set audio latency. Recommended", "自动设置音频延迟。推荐"}},
+    {"Enable DSP", {"Enable DSP", "启用 DSP"}},
+    {"Enable the Dreamcast Digital Sound Processor. Only recommended on fast platforms", {"Enable the Dreamcast Digital Sound Processor. Only recommended on fast platforms", "启用 Dreamcast 数字声音处理器。仅推荐在快速平台上使用"}},
+    
+    // Controls
+    {"Dreamcast Controls", {"Dreamcast Controls", "Dreamcast 控制"}},
+    {"Dreamcast Devices", {"Dreamcast Devices", "Dreamcast 设备"}},
+    {"Arcade Controls", {"Arcade Controls", "街机控制"}},
+    {"Controller Type:", {"Controller Type:", "控制器类型："}},
+    {"Mouse sensitivity", {"Mouse sensitivity", "鼠标灵敏度"}},
+    {"Haptic feedback power", {"Haptic feedback power", "触觉反馈强度"}},
+    {"Virtual gamepad buttons transparency", {"Virtual gamepad buttons transparency", "虚拟手柄按钮透明度"}},
+    {"Left Thumbstick:", {"Left Thumbstick:", "左摇杆："}},
+    {"Left thumbstick not used", {"Left thumbstick not used", "左摇杆未使用"}},
+    {"Use the left thumbstick horizontal axis only", {"Use the left thumbstick horizontal axis only", "仅使用左摇杆水平轴"}},
+    {"Use the left thumbstick horizontal and vertical axes", {"Use the left thumbstick horizontal and vertical axes", "使用左摇杆水平和垂直轴"}},
+    {"Rumble power", {"Rumble power", "震动强度"}},
+    {"Minimum deflection to register as input", {"Minimum deflection to register as input", "注册输入的最小偏转"}},
+    {"Value sent to the game at 100% thumbstick deflection. ", {"Value sent to the game at 100% thumbstick deflection. ", "100% 摇杆偏转时发送给游戏的值"}},
+    {"Values greater than 100% will saturate before full deflection of the thumbstick.", {"Values greater than 100% will saturate before full deflection of the thumbsticks.", "大于 100% 的值将在摇杆完全偏转前饱和"}},
+    {"Buttons", {"Buttons", "按钮"}},
+    {"System Buttons", {"System Buttons", "系统按钮"}},
+    {"Directions", {"Directions", "方向"}},
+    {"Thumbsticks", {"Thumbsticks", "摇杆"}},
+    {"Triggers", {"Triggers", "扳机"}},
+    {"Deselect to play as player 2", {"Deselect to play as player 2", "取消选择以玩家 2 身份游戏"}},
+    {"Bypass Emulated Keyboard", {"Bypass Emulated Keyboard", "绕过模拟键盘"}},
+    {"Densha de Go! Controller", {"Densha de Go! Controller", "电车 GO 控制器"}},
+    {"Fishing Controller", {"Fishing Controller", "钓鱼控制器"}},
+    {"Maracas Controller", {"Maracas Controller", "沙锤控制器"}},
+    {"Pop'n Music controller", {"Pop'n Music controller", "Pop'n Music 控制器"}},
+    {"Racing Controller", {"Racing Controller", "赛车控制器"}},
+    {"Twin Stick", {"Twin Stick", "双摇杆"}},
+    {"Light Gun", {"Light Gun", "光枪"}},
+    {"Keyboard", {"Keyboard", "键盘"}},
+    {"Mouse", {"Mouse", "鼠标"}},
+    {"Microphone", {"Microphone", "麦克风"}},
+    {"Sega Controller", {"Sega Controller", "世嘉控制器"}},
+    {"Sega VMU", {"Sega VMU", "世嘉 VMU"}},
+    {"Vibration Pack", {"Vibration Pack", "振动包"}},
+    {"Panther DC/Full Controller", {"Panther DC/Full Controller", "Panther DC/全控制器"}},
+    {"Arcade/Ascii Stick", {"Arcade/Ascii Stick", "街机/Ascii 摇杆"}},
+    {"Map", {"Map", "映射"}},
+    {"Unmap", {"Unmap", "取消映射"}},
+    {"Reset...", {"Reset...", "重置..."}},
+    {"Are you sure you want to reset the mappings to default?", {"Are you sure you want to reset the mappings to default?", "确定要将映射重置为默认值吗？"}},
+    {"Edit Layout", {"Edit Layout", "编辑布局"}},
+    {"Port %c", {"Port %c", "端口 %c"}},
+    {"Waiting for control '%s'...", {"Waiting for control '%s'...", "等待控制 '%s'..."}},
+    {"Time out in %d s", {"Time out in %d s", "超时 %d 秒"}},
+    {"Current inputs: ", {"Current inputs: ", "当前输入："}},
+    
+    // Network
+    {"Network Type", {"Network Type", "网络类型"}},
+    {"Network Options", {"Network Options", "网络选项"}},
+    {"GGPO", {"GGPO", "GGPO 回滚"}},
+    {"Enable networking using GGPO", {"Enable networking using GGPO", "使用 GGPO 启用网络"}},
+    {"Naomi", {"Naomi", "Naomi"}},
+    {"Enable networking for supported Naomi and Atomiswave games", {"Enable networking for supported Naomi and Atomiswave games", "为支持的 Naomi 和 Atomiswave 游戏启用网络"}},
+    {"Create a local server for Naomi network games", {"Create a local server for Naomi network games", "为 Naomi 网络游戏创建本地服务器"}},
+    {"Battle Cable", {"Battle Cable", "对战线缆"}},
+    {"Emulate the Taisen (Battle) null modem cable for games that support it", {"Emulate the Taisen (Battle) null modem cable for games that support it", "为支持的游戏模拟对战零调制解调器线缆"}},
+    {"Emulate the Ethernet Broadband Adapter (BBA) instead of the Modem", {"Emulate the Ethernet Broadband Adapter (BBA) instead of the Modem", "模拟以太网宽带适配器 (BBA) 而非调制解调器"}},
+    {"Connect to expansion devices such as VMUs over local TCP.", {"Connect to expansion devices such as VMUs over local TCP.", "通过本地 TCP 连接到 VMU 等扩展设备"}},
+    {"Local Port", {"Local Port", "本地端口"}},
+    {"The local UDP port to use", {"The local UDP port to use", "要使用的本地 UDP 端口"}},
+    {"Code", {"Code", "代码"}},
+    {"Choose a unique word or number and share it with your opponent.", {"Choose a unique word or number and share it with your opponent.", "选择一个唯一的单词或数字并与对手分享"}},
+    {"Match Code", {"Match Code", "匹配码"}},
+    {"The peer to connect to. Leave blank to find a player automatically on the default port", {"The peer to connect to. Leave blank to find a player automatically on the default port", "要连接的对等端。留空以在默认端口自动查找玩家"}},
+    {"The server to connect to. Leave blank to find a server automatically on the default port", {"The server to connect to. Leave blank to find a server automatically on the default port", "要连接的服务器。留空以在默认端口自动查找服务器"}},
+    {"Only used for Maximum Speed. One of the peer must be master.", {"Only used for Maximum Speed. One of the peer must be master.", "仅用于最大速度。其中一个对等端必须是主机"}},
+    {"Sets Frame Delay, advisable for sessions with ping >100 ms", {"Sets Frame Delay, advisable for sessions with ping >100 ms", "设置帧延迟，建议在延迟 >100ms 时使用"}},
+    {"Display network statistics on screen", {"Display network statistics on screen", "在屏幕上显示网络统计"}},
+    {"Chat Window Timeout (seconds)", {"Chat Window Timeout (秒)", "聊天窗口超时（秒）"}},
+    {"Sets duration that chat window stays open after new message is received.", {"Sets duration that chat window stays open after new message is received.", "设置收到新消息后聊天窗口保持打开的时间"}},
+    {"Multiboard Screens", {"Multiboard Screens", "多板屏幕"}},
+    {"Connected", {"Connected", "已连接"}},
+    {"Disconnected", {"Disconnected", "未连接"}},
+    {"Connect", {"Connect", "连接"}},
+    {"Disconnect", {"Disconnect", "断开"}},
+    {"Starting...", {"Starting...", "正在启动..."}},
+    {"Starting Network...", {"Starting Network...", "正在启动网络..."}},
+    {"Waiting at meeting point...", {"Waiting at meeting point...", "在集合点等待..."}},
+    {"Press Start to start the game now.", {"Press Start to start the game now.", "按 Start 键开始游戏"}},
+    {"Insert Card", {"Insert Card", "插入卡片"}},
+    {"Password", {"Password", "密码"}},
+    {"Login", {"Login", "登录"}},
+    {"Logout", {"Logout", "退出登录"}},
+    {"Authenticating...", {"Authenticating...", "正在认证..."}},
+    {"Authentication successful", {"Authentication successful", "认证成功"}},
+    {"Custom Driver:", {"Custom Driver:", "自定义驱动："}},
+    {"Upload Custom Driver", {"Upload Custom Driver", "上传自定义驱动"}},
+    {"Use Default Driver", {"Use Default Driver", "使用默认驱动"}},
+    {"DreamLink status: %s", {"DreamLink status: %s", "DreamLink 状态：%s"}},
+    {"Connect DreamLink", {"Connect DreamLink", "连接 DreamLink"}},
+    {"Disconnect\nDreamLink", {"Disconnect\nDreamLink", "断开\nDreamLink"}},
+    
+    // Advanced
+    {"CPU Mode", {"CPU Mode", "CPU 模式"}},
+    {"Dynarec", {"Dynarec", "动态重编译"}},
+    {"Interpreter", {"Interpreter", "解释器"}},
+    {"Use the dynamic recompiler. Recommended in most cases", {"Use the dynamic recompiler. Recommended in most cases", "使用动态重编译器。大多数情况下推荐"}},
+    {"Use the interpreter. Very slow but may help in case of a dynarec problem", {"Use the interpreter. Very slow but may help in case of a dynarec problem", "使用解释器。非常慢，但在动态重编译出问题时可能有帮助"}},
+    {"SH4 Clock", {"SH4 Clock", "SH4 时钟频率"}},
+    {"HLE BIOS", {"HLE BIOS", "HLE BIOS"}},
+    {"Force high-level BIOS emulation", {"Force high-level BIOS emulation", "强制高级 BIOS 模拟"}},
+    {"Very slow and incompatible with upscaling and wide screen.", {"Very slow and incompatible with upscaling and wide screen.", "非常慢且与放大和宽屏不兼容"}},
+    {"Fully accurate VRAM framebuffer emulation. Helps games that directly access the framebuffer for special effects. ", {"Fully accurate VRAM framebuffer emulation. Helps games that directly access the framebuffer for special effects. ", "完全精确的 VRAM 帧缓冲模拟。帮助直接访问帧缓冲实现特效的游戏"}},
+    {"Copy Rendered Textures to VRAM", {"Copy Rendered Textures to VRAM", "复制渲染纹理到 VRAM"}},
+    {"Copy rendered-to textures back to VRAM. Slower but accurate", {"Copy rendered-to textures back to VRAM. Slower but accurate", "将渲染纹理复制回 VRAM。较慢但精确"}},
+    {"Enable modifier volumes, usually used for shadows", {"Enable modifier volumes, usually used for shadows", "启用修改器体积，通常用于阴影"}},
+    {"Useful to avoid flashing screen or glitchy videos. Not recommended on slow platforms", {"Useful to avoid flashing screen or glitchy videos. Not recommended on slow platforms", "有助于避免闪屏或视频故障。不推荐在慢速平台上使用"}},
+    {"Fix Upscale Bleeding Edge", {"Fix Upscale Bleeding Edge", "修复放大渗色"}},
+    {"Helps with texture bleeding case when upscaling. Disabling it can help if pixels are warping when upscaling in 2D games (MVC2, CVS, KOF, etc.)", {"Helps with texture bleeding case when upscaling. Disabling it can help if pixels are warping when upscaling in 2D games (MVC2, CVS, KOF, etc.)", "放大时帮助解决纹理渗色。如果 2D 游戏放大时像素变形，禁用可能有帮助"}},
+    {"Native Depth Interpolation", {"Native Depth Interpolation", "原生深度插值"}},
+    {"Helps with texture corruption and depth issues on AMD GPUs. Can also help Intel GPUs in some cases.", {"Helps with texture corruption and depth issues on AMD GPUs. Can also help Intel GPUs in some cases.", "帮助解决 AMD GPU 的纹理损坏和深度问题。某些情况下也可帮助 Intel GPU"}},
+    {"Load Custom Textures", {"Load Custom Textures", "加载自定义纹理"}},
+    {"Load custom/high-res textures from data/textures/<game id>", {"Load custom/high-res textures from data/textures/<game id>", "从 data/textures/<game id> 加载自定义/高分辨率纹理"}},
+    {"Preload Custom Textures", {"Preload Custom Textures", "预加载自定义纹理"}},
+    {"Preload custom textures at game start. May improve performance but increases memory usage", {"Preload custom textures at game start. May improve performance but increases memory usage", "游戏开始时预加载自定义纹理。可能提高性能但增加内存使用"}},
+    {"Linear Interpolation", {"Linear Interpolation", "线性插值"}},
+    {"Duplicate frames", {"Duplicate frames", "重复帧"}},
+    {"Duplicate frames on high refresh rate monitors (120 Hz and higher)", {"Duplicate frames on high refresh rate monitors (120 Hz and higher)", "在高刷新率显示器上重复帧（120Hz 及以上）"}},
+    {"Shadows", {"Shadows", "阴影"}},
+    {"Fog", {"Fog", "雾效"}},
+    {"Enable fog effects", {"Enable fog effects", "启用雾效"}},
+    {"Delay Frame Swapping", {"Delay Frame Swapping", "延迟帧交换"}},
+    {"Show FPS Counter", {"Show FPS Counter", "显示 FPS 计数器"}},
+    {"Show on-screen frame/sec counter", {"Show on-screen frame/sec counter", "在屏幕上显示帧率计数器"}},
+    {"Show VMU In-game", {"Show VMU In-game", "游戏中显示 VMU"}},
+    {"Show the VMU LCD screens while in-game", {"Show the VMU LCD screens while in-game", "在游戏中显示 VMU LCD 屏幕"}},
+    {"Full Framebuffer Emulation", {"Full Framebuffer Emulation", "完整帧缓冲模拟"}},
+    {"Rotate Screen 90°", {"Rotate Screen 90°", "旋转屏幕 90°"}},
+    {"Rotate the screen 90° counterclockwise", {"Rotate the screen 90° counterclockwise", "逆时针旋转屏幕 90°"}},
+    {"Widescreen", {"Widescreen", "宽屏"}},
+    {"Super Widescreen", {"Super Widescreen", "超宽屏"}},
+    {"Widescreen Game Cheats", {"Widescreen Game Cheats", "宽屏游戏金手指"}},
+    {"Scale down before sending", {"Scale down before sending", "发送前缩小"}},
+    {"Video Routing (Spout)", {"Video Routing (Spout)", "视频路由 (Spout)"}},
+    {"Video Routing (Syphon)", {"Video Routing (Syphon)", "视频路由 (Syphon)"}},
+    {"Send video content to another program", {"Send video content to another program", "将视频内容发送到另一个程序"}},
+    {"Could increase performance when sharing a smaller texture, YMMV", {"Could increase performance when sharing a smaller texture, YMMV", "共享较小纹理时可能提高性能，效果因情况而异"}},
+    
+    // Audio
+    {"Enable DSP", {"Enable DSP", "启用 DSP"}},
+    {"Enable the Dreamcast Digital Sound Processor. Only recommended on fast platforms", {"Enable the Dreamcast Digital Sound Processor. Only recommended on fast platforms", "启用 Dreamcast 数字声音处理器。仅推荐在快速平台上使用"}},
+    {"Enable VMU Sounds", {"Enable VMU Sounds", "启用 VMU 声音"}},
+    {"Play VMU beeps when enabled.", {"Play VMU beeps when enabled.", "启用时播放 VMU 蜂鸣声"}},
+    {"Automatic Latency", {"Automatic Latency", "自动延迟"}},
+    {"Automatically set audio latency. Recommended", {"Automatically set audio latency. Recommended", "自动设置音频延迟。推荐"}},
+    
+    // Controls
+    {"Use Raw Input", {"Use Raw Input", "使用原始输入"}},
+    {"Supports multiple pointing devices (mice, light guns) and keyboards", {"Supports multiple pointing devices (mice, light guns) and keyboards", "支持多个指向设备（鼠标、光枪）和键盘"}},
+    {"Use Physical VMU Memory", {"Use Physical VMU Memory", "使用物理 VMU 内存"}},
+    {"Enables direct read/write access to physical VMU memory via DreamPicoPort/DreamConn. ", {"Enables direct read/write access to physical VMU memory via DreamPicoPort/DreamConn. ", "通过 DreamPicoPort/DreamConn 启用对物理 VMU 内存的直接读写访问"}},
+    {"Use Network Expansion Devices", {"Use Network Expansion Devices", "使用网络扩展设备"}},
+    {"Connect to expansion devices such as VMUs over local TCP.", {"Connect to expansion devices such as VMUs over local TCP.", "通过本地 TCP 连接到 VMU 等扩展设备"}},
+    {"Per Game VMU A1", {"Per Game VMU A1", "每游戏 VMU A1"}},
+    {"When enabled, each game has its own VMU on port 1 of controller A.", {"When enabled, each game has its own VMU on port 1 of controller A.", "启用时，每个游戏在控制器 A 的端口 1 上有自己的 VMU"}},
+    
+    // Network
+    {"Play as Player 1", {"Play as Player 1", "以玩家 1 身份游戏"}},
+    {"Enable Chat", {"Enable Chat", "启用聊天"}},
+    {"Open the chat window when a chat message is received", {"Open the chat window when a chat message is received", "收到聊天消息时打开聊天窗口"}},
+    {"Enable Chat Window Timeout", {"Enable Chat Window Timeout", "启用聊天窗口超时"}},
+    {"Automatically close chat window after 20 seconds", {"Automatically close chat window after 20 seconds", "20 秒后自动关闭聊天窗口"}},
+    {"Network Statistics", {"Network Statistics", "网络统计"}},
+    {"Display network statistics on screen", {"Display network statistics on screen", "在屏幕上显示网络统计"}},
+    {"Act as Server", {"Act as Server", "作为服务器"}},
+    {"Act as Master", {"Act as Master", "作为主机"}},
+    {"Enable UPnP", {"Enable UPnP", "启用 UPnP"}},
+    {"Automatically configure your network router for netplay", {"Automatically configure your network router for netplay", "自动配置网络路由器以进行网络对战"}},
+    {"Broadcast Digital Outputs", {"Broadcast Digital Outputs", "广播数字输出"}},
+    {"Broadcast digital outputs and force-feedback state on TCP port 8000. ", {"Broadcast digital outputs and force-feedback state on TCP port 8000. ", "在 TCP 端口 8000 上广播数字输出和力反馈状态"}},
+    {"Broadband Adapter Emulation", {"Broadband Adapter Emulation", "宽带适配器模拟"}},
+    {"Emulate the Ethernet Broadband Adapter (BBA) instead of the Modem", {"Emulate the Ethernet Broadband Adapter (BBA) instead of the Modem", "模拟以太网宽带适配器 (BBA) 而非调制解调器"}},
+    {"Use DCNet", {"Use DCNet", "使用 DCNet"}},
+    {"Use the DCNet cloud service for Dreamcast Internet access.", {"Use the DCNet cloud service for Dreamcast Internet access.", "使用 DCNet 云服务进行 Dreamcast 互联网访问"}},
+    
+    // Paths
+    {"Bios Folder", {"Bios Folder", "BIOS 文件夹"}},
+    {"BIOS Folders", {"BIOS Folders", "BIOS 文件夹"}},
+    {"Folders containing BIOS/Flash files (e.g. dc_boot.bin, dc_flash.bin) and arcade BIOS", {"Folders containing BIOS/Flash files (e.g. dc_boot.bin, dc_flash.bin) and arcade BIOS", "包含 BIOS/Flash 文件（如 dc_boot.bin、dc_flash.bin）和街机 BIOS 的文件夹"}},
+    {"Content Location", {"Content Location", "内容位置"}},
+    {"Custom Paths", {"Custom Paths", "自定义路径"}},
+    {"Home Folder", {"Home Folder", "主文件夹"}},
+    {"Data Folder", {"Data Folder", "数据文件夹"}},
+    {"Game Save Folder", {"Game Save Folder", "游戏存档文件夹"}},
+    {"VMU Folder", {"VMU Folder", "VMU 文件夹"}},
+    {"Savestate Folders", {"Savestate Folders", "存档文件夹"}},
+    {"Box Art Folder", {"Box Art Folder", "封面图文件夹"}},
+    {"Cheat Folders", {"Cheat Folders", "金手指文件夹"}},
+    {"Controller Mapping Folders", {"Controller Mapping Folders", "控制器映射文件夹"}},
+    {"Texture Pack Folders", {"Texture Pack Folders", "纹理包文件夹"}},
+    {"Texture Dump Folder", {"Texture Dump Folder", "纹理转储文件夹"}},
+    {"Folder for game save data (e.g. arcade NVRAM)", {"Folder for game save data (e.g. arcade NVRAM)", "游戏存档数据文件夹（如街机 NVRAM）"}},
+    {"Folder where VMU (.bin) saves are stored", {"Folder where VMU (.bin) saves are stored", "VMU (.bin) 存档存储文件夹"}},
+    {"Folders for save states. First path is used for new states; all are searched when loading", {"Folders for save states. First path is used for new states; all are searched when loading", "存档文件夹。第一个路径用于新状态；加载时搜索所有路径"}},
+    {"Folder containing box art images (png/jpg). If empty, Flycast will use the default Home Folder/boxart for downloads and generated art", {"Folder containing box art images (png/jpg). If empty, Flycast will use the default Home Folder/boxart for downloads and generated art", "包含封面图的文件夹。如果为空，Flycast 将使用默认主文件夹/boxart"}},
+    {"Folders containing cheat files (.cht/.txt) named with the game ID. Flycast will auto-load matching files if present", {"Folders containing cheat files (.cht/.txt) named with the game ID. Flycast will auto-load matching files if present", "包含金手指文件的文件夹。Flycast 会自动加载匹配文件"}},
+    {"Folders containing controller mapping files (.cfg). The emulator also looks in Home Folder/mappings. Per-game mappings are suffixed with _<gameId>.cfg", {"Folders containing controller mapping files (.cfg). The emulator also looks in Home Folder/mappings. Per-game mappings are suffixed with _<gameId>.cfg", "包含控制器映射文件的文件夹。每游戏映射以 _<gameId>.cfg 为后缀"}},
+    {"Folders containing textures/<gameId> or <gameId> under a textures subfolder", {"Folders containing textures/<gameId> or <gameId> under a textures subfolder", "包含纹理包的文件夹"}},
+    {"Folder where texture dumps are saved. Game-specific subfolders will be created automatically", {"Folder where texture dumps are saved. Game-specific subfolders will be created automatically", "纹理转储保存的文件夹。将自动创建游戏特定子文件夹"}},
+    {"The folder containing BIOS files, as well as saved VMUs and states", {"The folder containing BIOS files, as well as saved VMUs and states", "包含 BIOS 文件以及保存的 VMU 和状态的文件夹"}},
+    {"The folder where Flycast saves configuration files and VMUs. BIOS files should be in a subfolder named \"", {"The folder where Flycast saves configuration files and VMUs. BIOS files should be in a subfolder named \"", "Flycast 保存配置文件和 VMU 的文件夹"}},
+    {"The folders where your games are stored", {"The folders where your games are stored", "游戏存储的文件夹"}},
+    {"Add Game Folder", {"Add Game Folder", "添加游戏文件夹"}},
+    {"Rescan Content", {"Rescan Content", "重新扫描内容"}},
+    {"Add##ContentLocation", {"Add##ContentLocation", "添加"}},
+    
+    // Video - Additional
+    {"Aspect Ratio", {"Aspect Ratio", "宽高比"}},
+    {"Stretch the screen horizontally", {"Stretch the screen horizontally", "水平拉伸屏幕"}},
+    {"Per Strip", {"Per Strip", "逐条带"}},
+    {"Per Triangle", {"Per Triangle", "逐三角形"}},
+    {"Per Pixel", {"Per Pixel", "逐像素"}},
+    {"Transparent Sorting", {"Transparent Sorting", "透明排序"}},
+    {"Frame Skipping", {"Frame Skipping", "跳帧"}},
+    {"Graphics API", {"Graphics API", "图形 API"}},
+    {"DirectX", {"DirectX", "DirectX"}},
+    {"Output vertical resolution", {"Output vertical resolution", "输出垂直分辨率"}},
+    {"Crosshair", {"Crosshair", "准星"}},
+    {"Crosshair color", {"Crosshair color", "准星颜色"}},
+    {"Saturation", {"Saturation", "饱和度"}},
+    {"VBlank", {"VBlank", "垂直消隐"}},
+    {"Render Times", {"Render Times", "渲染时间"}},
+    
+    // Audio - Additional  
+    {"Latency", {"Latency", "延迟"}},
+    {"The audio driver to use", {"The audio driver to use", "要使用的音频驱动"}},
+    
+    // Controls - Additional
+    {"Physical Devices", {"Physical Devices", "物理设备"}},
+    {"Gamepad Settings", {"Gamepad Settings", "手柄设置"}},
+    {"Controller Mapping", {"Controller Mapping", "控制器映射"}},
+    {"Map Control", {"Map Control", "映射控制"}},
+    {"Dead zone", {"Dead zone", "死区"}},
+    {"Saturation", {"Saturation", "饱和度"}},
+    {"Crosshair", {"Crosshair", "准星"}},
+    {"Crosshair color", {"Crosshair color", "准星颜色"}},
+    {"Bypass Emulated Keyboard", {"Bypass Emulated Keyboard", "绕过模拟键盘"}},
+    {"Densha de Go! Controller", {"Densha de Go! Controller", "电车 GO 控制器"}},
+    
+    // Network - Additional
+    {"Network Type", {"Network Type", "网络类型"}},
+    {"Network Options", {"Network Options", "网络选项"}},
+    {"Multiboard Screens", {"Multiboard Screens", "多板屏幕"}},
+    {"DNS", {"DNS", "DNS"}},
+    {"DNS server name or IP address", {"DNS server name or IP address", "DNS 服务器名称或 IP 地址"}},
+    {"Server", {"Server", "服务器"}},
+    {"Peer", {"Peer", "对等端"}},
+    {"Username", {"Username", "用户名"}},
+    {"ISP User Name", {"ISP User Name", "ISP 用户名"}},
+    {"Your peer IP address and optional port", {"Your peer IP address and optional port", "你的对等端 IP 地址和可选端口"}},
+    
+    // Advanced - Additional
+    {"Multi-threaded emulation", {"Multi-threaded emulation", "多线程模拟"}},
+    {"Run the emulated CPU and GPU on different threads", {"Run the emulated CPU and GPU on different threads", "在不同线程上运行模拟的 CPU 和 GPU"}},
+    {"Dreamcast 32MB RAM Mod", {"Dreamcast 32MB RAM Mod", "Dreamcast 32MB 内存 Mod"}},
+    {"Dump Textures", {"Dump Textures", "转储纹理"}},
+    {"Always dump textures that are already replaced by custom textures", {"Always dump textures that are already replaced by custom textures", "始终转储已被自定义纹理替换的纹理"}},
+    {"Dump Replaced Textures", {"Dump Replaced Textures", "转储替换的纹理"}},
+    {"Automatically Report Crashes", {"Automatically Report Crashes", "自动报告崩溃"}},
+    {"Lua Scripting", {"Lua Scripting", "Lua 脚本"}},
+    {"Lua Filename", {"Lua Filename", "Lua 文件名"}},
+    {"Logging", {"Logging", "日志"}},
+    {"Log Verbosity", {"Log Verbosity", "日志详细程度"}},
+    {"Log to File", {"Log to File", "日志写入文件"}},
+    {"LogToFile", {"LogToFile", "日志文件"}},
+    {"Log Server", {"Log Server", "日志服务器"}},
+    {"Verbosity", {"Verbosity", "详细程度"}},
+    {"Profiling", {"Profiling", "性能分析"}},
+    {"Profiler", {"Profiler", "分析器"}},
+    {"Debug", {"Debug", "调试"}},
+    {"Enable GDB", {"Enable GDB", "启用 GDB"}},
+    {"GDB debugging support, disables Dynarec and dramatically reduces performance when a debugger is connected.", {"GDB debugging support, disables Dynarec and dramatically reduces performance when a debugger is connected.", "GDB 调试支持，禁用动态重编译，连接调试器时性能大幅降低"}},
+    {"Wait for connection", {"Wait for connection", "等待连接"}},
+    {"Start emulation once the debugger is connected.", {"Start emulation once the debugger is connected.", "调试器连接后开始模拟"}},
+    {"Serial Console", {"Serial Console", "串行控制台"}},
+    {"Dump the Dreamcast serial console to stdout", {"Dump the Dreamcast serial console to stdout", "将 Dreamcast 串行控制台转储到 stdout"}},
+    {"Serial PTY", {"Serial PTY", "串行 PTY"}},
+    {"Requires the option \"Serial Console\" to work", {"Requires the option \"Serial Console\" to work", "需要 \"串行控制台\" 选项才能工作"}},
+    {"GDB port", {"GDB port", "GDB 端口"}},
+    {"Virtual memory addresses", {"Virtual memory addresses", "虚拟内存地址"}},
+    {"RAM adresses are not available until the emulation is started", {"RAM adresses are not available until the emulation is started", "RAM 地址在模拟启动前不可用"}},
+    {"Reset Vulkan", {"Reset Vulkan", "重置 Vulkan"}},
+    {"Multi-threaded emulation", {"Multi-threaded emulation", "多线程模拟"}},
+    {"Run the emulated CPU and GPU on different threads", {"Run the emulated CPU and GPU on different threads", "在不同线程上运行模拟的 CPU 和 GPU"}},
+    
+    // About - Additional
+    {"AA", {"AA", "抗锯齿"}},
+    {"ARM", {"ARM", "ARM"}},
+    
+    // Menu - Additional
+    {"Done", {"Done", "完成"}},
+    {"Make Game Config", {"Make Game Config", "创建游戏配置"}},
+    {"Delete Game Config", {"Delete Game Config", "删除游戏配置"}},
+    {"Confirm Reset", {"Confirm Reset", "确认重置"}},
+    {"Reveal in Finder", {"Reveal in Finder", "在文件管理器中显示"}},
+    {"Reload", {"Reload", "重新加载"}},
+    {"Reselect", {"Reselect", "重新选择"}},
+    {"Slot ", {"Slot ", "槽位 "}},
+    {"Filter", {"Filter", "筛选"}},
+    {"Barcode Card", {"Barcode Card", "条码卡"}},
+    {"Insert Card", {"Insert Card", "插入卡片"}},
+    {"Select Flycast Home Folder", {"Select Flycast Home Folder", "选择 Flycast 主文件夹"}},
+    {"Select a Content Folder", {"Select a Content Folder", "选择内容文件夹"}},
+    {"Select a PNG file", {"Select a PNG file", "选择 PNG 文件"}},
+    {"Select a custom GPU driver", {"Select a custom GPU driver", "选择自定义 GPU 驱动"}},
+    {"Invalid selection:\nFlycast cannot write to this folder.", {"Invalid selection:\nFlycast cannot write to this folder.", "无效选择：\nFlycast 无法写入此文件夹"}},
+    {"Cannot write in the 'data' directory", {"Cannot write in the 'data' directory", "无法写入 'data' 目录"}},
+    
+    // Common - Additional
+    {"Error", {"Error", "错误"}},
+    {"Warning", {"Warning", "警告"}},
+    {"Notice", {"Notice", "通知"}},
+    {"Info", {"Info", "信息"}},
+    {"Configuration", {"Configuration", "配置"}},
+    {"Import", {"Import", "导入"}},
+    
+    // Paths
+    {"Home Folder", {"Home Folder", "主文件夹"}},
+    {"The folder containing BIOS files, as well as saved VMUs and states", {"The folder containing BIOS files, as well as saved VMUs and states", "包含 BIOS 文件以及保存的 VMU 和状态的文件夹"}},
+    {"Data Folder", {"Data Folder", "数据文件夹"}},
+    {"The folder where Flycast saves configuration files and VMUs. BIOS files should be in a subfolder named \"", {"The folder where Flycast saves configuration files and VMUs. BIOS files should be in a subfolder named \"", "Flycast 保存配置文件和 VMU 的文件夹。BIOS 文件应在名为 \" 的子文件夹中"}},
+    {"Bios Folder", {"Bios Folder", "BIOS 文件夹"}},
+    {"Folders containing BIOS/Flash files (e.g. dc_boot.bin, dc_flash.bin) and arcade BIOS", {"Folders containing BIOS/Flash files (e.g. dc_boot.bin, dc_flash.bin) and arcade BIOS", "包含 BIOS/Flash 文件（如 dc_boot.bin、dc_flash.bin）和街机 BIOS 的文件夹"}},
+    {"Content Location", {"Content Location", "内容位置"}},
+    {"The folders where your games are stored", {"The folders where your games are stored", "游戏存储的文件夹"}},
+    {"Custom Paths", {"Custom Paths", "自定义路径"}},
+    {"Folder for game save data (e.g. arcade NVRAM)", {"Folder for game save data (e.g. arcade NVRAM)", "游戏保存数据文件夹（如街机 NVRAM）"}},
+    {"Folder where VMU (.bin) saves are stored", {"Folder where VMU (.bin) saves are stored", "VMU (.bin) 存档存储文件夹"}},
+    {"Folders for save states. First path is used for new states; all are searched when loading", {"Folders for save states. First path is used for new states; all are searched when loading", "存档状态文件夹。第一个路径用于新状态；加载时搜索所有路径"}},
+    {"Folder containing box art images (png/jpg). If empty, Flycast will use the default Home Folder/boxart for downloads and generated art", {"Folder containing box art images (png/jpg). If empty, Flycast will use the default Home Folder/boxart for downloads and generated art", "包含封面图（png/jpg）的文件夹。如果为空，Flycast 将使用默认主文件夹/boxart"}},
+    {"Folders containing cheat files (.cht/.txt) named with the game ID. Flycast will auto-load matching files if present", {"Folders containing cheat files (.cht/.txt) named with the game ID. Flycast will auto-load matching files if present", "包含以游戏 ID 命名的金手指文件（.cht/.txt）的文件夹。Flycast 会自动加载匹配文件"}},
+    {"Folders containing controller mapping files (.cfg). The emulator also looks in Home Folder/mappings. Per-game mappings are suffixed with _<gameId>.cfg", {"Folders containing controller mapping files (.cfg). The emulator also looks in Home Folder/mappings. Per-game mappings are suffixed with _<gameId>.cfg", "包含控制器映射文件（.cfg）的文件夹。模拟器也会在主文件夹/mappings 中查找。每游戏映射以 _<gameId>.cfg 为后缀"}},
+    {"Folders containing textures/<gameId> or <gameId> under a textures subfolder", {"Folders containing textures/<gameId> or <gameId> under a textures subfolder", "包含 textures/<gameId> 或 textures 子文件夹下的 <gameId> 的文件夹"}},
+    {"Folder where texture dumps are saved. Game-specific subfolders will be created automatically", {"Folder where texture dumps are saved. Game-specific subfolders will be created automatically", "纹理转储保存的文件夹。将自动创建游戏特定子文件夹"}},
+    {"Add Game Folder", {"Add Game Folder", "添加游戏文件夹"}},
+    {"Rescan Content", {"Rescan Content", "重新扫描内容"}},
+    {"Add##ContentLocation", {"Add##ContentLocation", "添加##内容位置"}},
+    
+    // Menu
+    {"Resume", {"Resume", "继续"}},
+    {"Settings", {"Settings", "设置"}},
+    {"Load State", {"Load State", "读取存档"}},
+    {"Save State", {"Save State", "保存存档"}},
+    {"Exit", {"Exit", "退出"}},
+    {"Close Game", {"Close Game", "关闭游戏"}},
+    {"Achievements", {"Achievements", "成就"}},
+    {"Insert Disk", {"Insert Disk", "插入光盘"}},
+    {"Eject Disk", {"Eject Disk", "弹出光盘"}},
+    {"Make Game Config", {"Make Game Config", "创建游戏配置"}},
+    {"Delete Game Config", {"Delete Game Config", "删除游戏配置"}},
+    {"Reveal in Finder", {"Reveal in Finder", "在文件管理器中显示"}},
+    {"Choose Image...", {"Choose Image...", "选择图片..."}},
+    {"GAMES", {"GAMES", "游戏"}},
+    {"Loading...", {"Loading...", "加载中..."}},
+    {"Preparing game...", {"Preparing game...", "准备游戏中..."}},
+    {"Game load failed", {"Game load failed", "游戏加载失败"}},
+    {"Fatal Error", {"Fatal Error", "致命错误"}},
+    {"Flycast has stopped.\n\n", {"Flycast has stopped.\n\n", "Flycast 已停止。\n\n"}},
+    {"Your game list is empty", {"Your game list is empty", "游戏列表为空"}},
+    {"No screenshot available", {"No screenshot available", "无截图可用"}},
+    {"Save Screenshot", {"Save Screenshot", "保存截图"}},
+    {"Error saving screenshot", {"Error saving screenshot", "保存截图错误"}},
+    {"Select Flycast Home Folder", {"Select Flycast Home Folder", "选择 Flycast 主文件夹"}},
+    {"Select a Content Folder", {"Select a Content Folder", "选择内容文件夹"}},
+    {"Select a PNG file", {"Select a PNG file", "选择 PNG 文件"}},
+    {"Select a custom GPU driver", {"Select a custom GPU driver", "选择自定义 GPU 驱动"}},
+    {"Invalid selection:\nFlycast cannot write to this folder.", {"Invalid selection:\nFlycast cannot write to this folder.", "无效选择：\nFlycast 无法写入此文件夹"}},
+    {"Cannot write in the 'data' directory", {"Cannot write in the 'data' directory", "无法写入 'data' 目录"}},
+    {"Do you want to reset Vulkan to use new driver?", {"Do you want to reset Vulkan to use new driver?", "是否重置 Vulkan 以使用新驱动？"}},
+    {"Reload", {"Reload", "重新加载"}},
+    {"Fast-forward", {"Fast-forward", "快进"}},
+    {"View", {"View", "查看"}},
+    {"Menu", {"Menu", "菜单"}},
+    {"Select ", {"Select ", "选择 "}},
+    {"Playing ", {"Playing ", "正在玩 "}},
+    
+    // System
+    {"Gamepad", {"Gamepad", "手柄"}},
+    {"Arcade / Hit Box", {"Arcade / Hit Box", "街机 / Hit Box"}},
+    {"Normal", {"Normal", "普通"}},
+    {"Power", {"Power", "电源"}},
+    {"Export", {"Export", "导出"}},
+    {"1 (Twin)", {"1 (Twin)", "1（双屏）"}},
+    {"3 (Deluxe)", {"3 (Deluxe)", "3（豪华）"}},
+    {"One screen configuration (F355 Twin)", {"One screen configuration (F355 Twin)", "单屏配置（F355 双屏）"}},
+    {"Three screens configuration", {"Three screens configuration", "三屏配置"}},
+    {" MAME option. Arcade games only.", {" MAME option. Arcade games only.", " MAME 选项。仅限街机游戏"}},
+    
+    // About
+    {"Version: %s", {"Version: %s", "版本：%s"}},
+    {"Build Date: %s", {"Build Date: %s", "构建日期：%s"}},
+    {"Git Hash: %s", {"Git Hash: %s", "Git 哈希：%s"}},
+    {"CPU: %s", {"CPU: %s", "CPU：%s"}},
+    {"Operating System: %s", {"Operating System: %s", "操作系统：%s"}},
+    {"JIT Status: %s", {"JIT Status: %s", "JIT 状态：%s"}},
+    {"Driver Name: %s", {"Driver Name: %s", "驱动名称：%s"}},
+    
+    // Cable
+    {"VGA", {"VGA", "VGA"}},
+    {"RGB Component", {"RGB Component", "RGB 分量"}},
+    {"TV Composite", {"TV Composite", "TV 复合"}},
+    
+    // Regions
+    {"Japan", {"Japan", "日本"}},
+    {"USA", {"USA", "美国"}},
+    {"Europe", {"Europe", "欧洲"}},
+    {"Korea", {"Korea", "韩国"}},
+    
+    // Languages
+    {"Japanese", {"Japanese", "日语"}},
+    {"English", {"English", "英语"}},
+    {"German", {"German", "德语"}},
+    {"French", {"French", "法语"}},
+    {"Spanish", {"Spanish", "西班牙语"}},
+    {"Italian", {"Italian", "意大利语"}},
+    
+    // Themes
+    {"Dark", {"Dark", "深色"}},
+    {"Light", {"Light", "浅色"}},
+    {"Aqua Chill", {"Aqua Chill", "水蓝"}},
+    {"High Contrast", {"High Contrast", "高对比度"}},
+    
+    // Common
+    {"On", {"On", "开启"}},
+    {"Off", {"Off", "关闭"}},
+    {"Yes", {"Yes", "是"}},
+    {"No", {"No", "否"}},
+    {"OK", {"OK", "确定"}},
+    {"Cancel", {"Cancel", "取消"}},
+    {"Confirm", {"Confirm", "确认"}},
+    {"Default", {"Default", "默认"}},
+    {"Use Default", {"Use Default", "使用默认"}},
+    {"None", {"None", "无"}},
+    {"Apply", {"Apply", "应用"}},
+    {"Done", {"Done", "完成"}},
+    {"Disabled", {"Disabled", "禁用"}},
+    {"Manual", {"Manual", "手动"}},
+    {"Automatic", {"Automatic", "自动"}},
+    {"Half", {"Half", "一半"}},
+    {"Full", {"Full", "完整"}},
+    {"Maximum", {"Maximum", "最大"}},
+    {"Performance", {"Performance", "性能"}},
+    {"Compatibility", {"Compatibility", "兼容性"}},
+    {"All", {"All", "全部"}},
+    {"Platform", {"Platform", "平台"}},
+    {"Emulator", {"Emulator", "模拟器"}},
+    {"Coin", {"Coin", "投币"}},
+    {"Start", {"Start", "开始"}},
+    {"Service", {"Service", "服务"}},
+    {"Test", {"Test", "测试"}},
+    {"Up", {"Up", "上"}},
+    {"Down", {"Down", "下"}},
+    {"Left", {"Left", "左"}},
+    {"Right", {"Right", "右"}},
+    {"Left Trigger", {"Left Trigger", "左扳机"}},
+    {"Right Trigger", {"Right Trigger", "右扳机"}},
+    {"Left Trigger 2", {"Left Trigger 2", "左扳机 2"}},
+    {"Right Trigger 2", {"Right Trigger 2", "右扳机 2"}},
+    {"Thumbstick Up", {"Thumbstick Up", "摇杆上"}},
+    {"Thumbstick Down", {"Thumbstick Down", "摇杆下"}},
+    {"Thumbstick Left", {"Thumbstick Left", "摇杆左"}},
+    {"Thumbstick Right", {"Thumbstick Right", "摇杆右"}},
+    {"Haptic", {"Haptic", "触觉"}},
+    {"Rumble", {"Rumble", "震动"}},
+    {"Local", {"Local", "本地"}},
+    {"Physical Devices", {"Physical Devices", "物理设备"}},
+    {"Per Pixel Settings", {"Per Pixel Settings", "逐像素设置"}},
+    {"Video connection type", {"Video connection type", "视频连接类型"}},
+    {"Dreamcast", {"Dreamcast", "Dreamcast"}},
+    {"Nintendo", {"Nintendo", "任天堂"}},
+    {"Sega", {"Sega", "世嘉"}},
+    {"Unknown", {"Unknown", "未知"}},
+    {"  Scanned %d folders but no game can be found!  ", {"  Scanned %d folders but no game can be found!  ", "  扫描了 %d 个文件夹但未找到游戏！  "}},
+};
+
+inline bool isChineseLocale() {
+    static int cached = -1;
+    if (cached == -1) {
+        setlocale(LC_ALL, "");
+        const char* locale = setlocale(LC_ALL, nullptr);
+        if (locale == nullptr || strcmp(locale, "C") == 0 || strcmp(locale, "POSIX") == 0)
+            locale = getenv("LANG");
+        cached = (locale != nullptr && strstr(locale, "zh") != nullptr) ? 1 : 0;
+    }
+    return cached == 1;
+}
+
+inline const char* tr(const char* english) {
+    if (!isChineseLocale())
+        return english;
+    
+    auto it = translations.find(english);
+    if (it != translations.end())
+        return it->second.zh;
+    return english;
+}
